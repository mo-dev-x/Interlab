{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schemas/configs/characterize_v1.schema.json",
  "title": "characterize job config v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["checkpoint_hash", "corpus_manifest_hash", "corpus_location", "judge"],
  "properties": {
    "checkpoint_hash": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
    "corpus_manifest_hash": {
      "description": "WP7 compliance fix: A7's own spec (§4) requires corpus_manifest(s) of the sample in `subject` -- the manifest for the corpus `corpus_location` is sampled from, resolved and linked into the emitted characterization_manifest.",
      "type": "string", "pattern": "^sha256:[0-9a-f]{64}$"
    },
    "corpus_location": { "type": "string", "pattern": "^(tamia|local|hf|wandb):.+$" },
    "n_docs": { "type": "integer", "minimum": 1 },
    "chat_slice_location": { "type": ["string", "null"], "pattern": "^(tamia|local|hf|wandb):.+$" },
    "n_chat_docs": { "type": "integer", "minimum": 1 },
    "judge": {
      "description": "D2 stub-judge resolution (WP4): only 'stub' is implemented; 'none' produces null autointerp fields. Production autointerpretation is researcher-gated, out of scope here.",
      "enum": ["stub", "none"]
    },
    "rng_seed": { "type": "integer" },
    "index_dir": { "type": "string" }
  }
}
