# Completion Ledger

## 2026-08-07 (Sprint Day 2) Three support lanes closed; anchor test is the sole open gate

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| D1.4-FEATURE-SHORTLIST | **Closed — 9 features frozen** | Lab Assistant B, n=33 adjudicated. Admitted: 250, 500, 2048, 2500, 3500, 4500, 11000, 12800, 900(low-conf). Optional 10th: 8000. Rejected on snippet evidence: 12345, 7777, 6000, 100, 10500, 13500, 9600, 7000, 400, 14000. Search route proven unavailable (`/api/explanation/search` → HTTP 405, POST-only). | Feature set is **final**. No further Neuronpedia hunting — targeted lookup is impossible read-only and the two negatives (geography, physical object) are bounded, not proven. Rejection rate 10/20 = 50%. |
| STORAGE-PREFLIGHT | **Closed — no escalation** | `/scratch` demonstrated ≥53-day survival on **both** mtime and atime (1,352 files, oldest 2026-06-15, still present; another user's dir at ~114 d). Sprint needs ~6 days → ~9× margin. Inode rationale confirmed: ~998K free on `/scratch` vs ~94K on `/project`. | Purge risk is settled. **Space (497 GiB), not purge, is the binding storage constraint** — the narrow pull directive stands. Written policy unverified (Alliance wiki bot-blocked); logged as a documentation gap, not a blocker. |
| SPRINT-ENV | **Closed — `~/sprint-venv` verified** | Job 397829, 4×H100, COMPLETED 0:0. torch 2.13.0+cc, numpy 2.4.2+cc, transformers 5.14.1+cc from wheelhouse; `transformer-lens==3.2.1` and `sae-lens==6.44.2` exact from PyPI (absent from wheelhouse at every version). `pip check` clean. All 12 interplab submodules import. | `~/interplab-venv` **byte-unchanged** (manifest sha256 `d0aa134b…af168d`, 20443 files, both before and after) — the ED-36 frozen rebuild survived. `arrow/25.0.0` must load **before** activation or pyarrow/datasets break. |
| PYPROJECT-ENV-DIVERGENCE | **Ruled — not a blocker, record openly** | `pyproject.toml` pins `transformers==5.12.1` / `accelerate==0.33.0`; env has `5.14.1+cc` / `1.14.0+cc`. Wheelhouse has no 5.12.1. | Sprint science runs **outside** the certification chain by PI directive, so the divergence is in-scope for the declared speed-for-provenance trade. **Do not edit `pyproject.toml`** — that is a frozen governance surface. State the divergence in the write-up. |
| LEGACY-SWEEP-HARNESS | **Newly identified gap** | `scripts/legacy/steering_experiment.py` is **poutine-hardcoded** throughout: `mentions_poutine`, `poutine_mention_rate`, `plot_scale_curve` labels. Per the dropped-poutine rule it is not reusable as-is. | D2 needs a Gemma harness written, not merely invoked. **Dispatched in parallel with the anchor test** rather than sequenced after it — this is the schedule recovery for Day 2. |
| CROSS-MODEL-ARCHITECTURE-CONFOUND | **Ruled by PM — claim scoped to existence and direction only** | ~~Qwen SAE: `d_in=5120`, `d_sae=81920`, expansion 16×~~ **CORRECTED 2026-08-07:** the Qwen instrument is **`rwu04lpb/final_400001024`** — layer 28, `d_in=5120`, `d_sae=163840`, TopK k=100, **expansion 32×**, 400M FineWeb tokens (`configs/backfill/inputs/rwu04lpb_runner_cfg.json`). The 81920/16× figures were `9odeg5hb`'s, a superseded pile-10k **base**-model checkpoint. Gemma Scope 2 L31: `d_in=3840`, width 16k, **JumpReLU**, expansion ~4.2×. | Expansion gap is **~7.6:1, not ~3.8:1** — understated 2× in an escalation whose whole purpose was judging architectural mismatch. Approved claim: *"the surface-form-detector skew reproduces across two models, two SAE architectures (TopK vs JumpReLU), two expansion ratios (32× vs ~4.2×), and two training provenances."* **Existence and direction only, never magnitude** — and it licenses nothing about any individual feature across models. |

**Sole open Day-1 gate:** D1.5 anchor test (Engineer, in flight). Day 1 does not close until it returns.

### 2026-08-07 later — G1 revoked; two structural facts discovered

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| G1-GEMMA-ACCESS | **REVOKED — was cleared on insufficient evidence** | `snapshot_download("google/gemma-3-12b-pt")` → `GatedRepoError: 401`. The earlier clear rested on a successful `config.json` fetch plus `whoami()` reporting `canReadGatedRepos: True`. Neither proves a **per-repo grant**; `canReadGatedRepos` is a platform capability flag, and a metadata fetch is not a weight fetch. **Orchestrator error — I accepted the capability flag as the grant.** | Sprint is blocked at the model itself. Two live hypotheses, one cheap test each, dispatched in parallel: (a) token orphaned by the `HF_HOME` move, (b) license not accepted on the model page. Failure was at HEAD-metadata stage — **no weight bytes transferred**, `/scratch` +3 files. |
| TAMIA-COMPUTE-NODES-OFFLINE | **Established fact** | Job 397854 ran 50m40s then died on `OSError: We couldn't connect to 'https://huggingface.co'` after `httpcore.ConnectTimeout` retries exhausted. Both cache-location checks **passed** — this was never a cache-path bug. Login node curls huggingface.co in ~90 ms. Matches the pre-existing June-2026 `/scratch/y/yazid/hf_cache` and `montreal_qwen.py`'s own `HF_HUB_OFFLINE=1` header. | **Every** Tamia job touching HF weights must pre-stage on the login node and run with `HF_HUB_OFFLINE=1` against a **local snapshot path**, never a `repo_id`. Applies to the D2 fan-out **and** the Week-2 Gradio tool. D2.1 work order amended accordingly. |
| HF_HOME-TOKEN-COUPLING | **Established fact** | `huggingface_hub/constants.py`: `HF_TOKEN_PATH = os.getenv("HF_TOKEN_PATH", os.path.join(HF_HOME, "token"))`. Repointing `HF_HOME` to `/scratch` makes the library look for the credential at `$HF_HOME/token`, not `~/.cache/huggingface/token`. | Presents as a repo-grant failure but is a credential failure. **401 = credential absent/invalid; 403 = credential fine, grant missing.** Distinguish before escalating. |
| G1-CAUSE | **Resolved — H1 confirmed by elimination** | Account holder confirms the model page reads *"You have been granted access to this model."* The grant **is** live, so H2 is dead and the orphaned token is the whole cause. The pre-authorized 2026-08-08 09:00 Qwen-depth pivot is **moot** — do not trigger it. | Fix is `HF_TOKEN` env var per PM. **Added constraint:** `sbatch` defaults to `--export=ALL`, so a token exported in `~/sprint-venv/bin/activate` propagates into every job environment and any `set -x` / `env` in a launcher writes it to a Slurm log on `/scratch` — a violation of the never-in-a-log rule. Jobs are offline and never need it: derive `HF_TOKEN` from the existing 600-mode file at activation (no second plaintext copy), and `unset HF_TOKEN` at the top of every job payload. |
| QWEN-SAE-IDENTITY-32x | **`docs/ablation_9056_spec.md` is CORRECT — the 16× figure was my error** | `configs/backfill/inputs/rwu04lpb_runner_cfg.json`: `d_in=5120`, `d_sae=163840` → **32×**, corroborated by `run_name: topk-163840-…` and `configs/sae_train_instruct.yaml:19 expansion_factor: 32`. The `81920` (16×) belongs to **different checkpoints**: `9odeg5hb` (the abandoned pile-10k run, `FEATURE_EXPERIMENT_LOG.md:20`, superseded by the FineWeb retrain in §6) and `hm03l7yz` (the L28×16 width-sweep point). | **No document needs correcting.** I read §1 of a log whose §6 supersedes it — the exact trap I had already written into the Lab Assistant brief. Authoritative Qwen instrument: **`rwu04lpb`, layer 28, `d_in=5120`, `d_sae=163840`, TopK k=100, 32×, 400M FineWeb tokens.** Expansion gap vs Gemma is **32× vs ~4.2× (~7.6×)**, not 3.8× — which **strengthens** the PM's capacity-as-alternative-explanation reasoning. |
| MATCHED-PREFIX-FALLBACK | **Pre-registered 2026-08-08, before any count exists in either column** | Adjudication is relay-bound and may not reach n=40 in both columns before the deadline. A partial column at unmatched n would break the matched-denominator design that the whole comparison rests on. | **If time runs out, both columns truncate to the first N features IN SEED ORDER**, where N = min(Gemma completed, Qwen completed). Seed order is content-neutral, so a prefix of a seeded uniform sequence is **still a uniform sample** — this is truncation, not selection. Report N explicitly and never as "n=40". **Rows already classified beyond N are retained and reported separately, never silently dropped and never used to extend one column past the other.** Decided before any composition is visible, so the stopping point cannot be chosen to favour a result. |
| CONTEXT-LENGTH-DIAGNOSTIC | **Pre-registered before any count exists** | Qwen chunks: median 2038, but **21.3 % fall below Gemma's shortest measured record (1269 chars)**. Short context plausibly drives `indeterminate`, which would inflate Qwen's non-numerator buckets — the original window artifact surviving at reduced amplitude. | **Primary diagnostic:** regress `indeterminate` (binary) on `log(char_len)` **within each column separately**. Tests whether the mechanism operates at all, without re-selecting the sample. **Secondary:** recompute Qwen's composition on rows ≥1269 chars only — *disclosed caveat:* chunk length is capped by document length, and document length correlates with document type, so this subset is **content-correlated, not a random subsample**. It is a sensitivity arm, never the primary. **No fix exists** if the relationship is found — a chunk shorter than 512 tokens means the document itself was shorter, so the missing context does not exist. The response is quantified disclosure, not correction. **Blocking prerequisite:** the 1269–2847 range is an estimate from **three Gemma features**; the real Gemma length distribution must be emitted before any overlap figure is reported. |
| SKEW-DENOMINATOR-PREREGISTRATION | **Binding, committed before the data exists** | PM ruling: browsing exposure — not index arbitrariness — is the selection threat, and it endangers **direction**, not only magnitude. Gemma skew denominator becomes a seeded uniform draw over `[0, 16384)`, seed 42, n=40, matching Qwen's. Sweep set untouched. | **Pre-registered:** the uniform draw is the **authoritative** skew statistic; **58 % (19/33) is an interim browsed figure, reported as superseded whichever direction the new number moves.** Choosing the headline after seeing it would launder the selection problem one level up, through the choice of statistic. Two rules make the draw actually uniform: dead/unexplained latents get a **class**, never a redraw (their count is itself a result); and the existing 33 are retained as the sweep-admission record, not discarded. |
| D3.4-CONTROL-INVARIANCE | **PASSED — at zero API cost** | Three existing Lodestar runs share an identical generation text-set (`fdd28c26a7`, n=104): `"cheese"`, `"cheese curds"`, and a **same-string replicate** supplying a judge-noise floor. Random-feature control sits at **exactly 1.00 under both concept strings at all six scales, zero movement**; baseline likewise; treated arm swings up to **4.17×**. Noise floor: same-string replicate ≤0.08, coherence (whose template contains no `target_concept`) ≤0.17. **Effect ≈86× noise.** | **Control-relative direction survives concept-string choice — D3's cross-model comparison rests on solid ground.** Direction is systematic and one-signed: the narrower string depresses at 6/6 scales, no sign flips, swing grows with dose (1.62× @40 → 4.17× @150). Mechanism legible: steering drives generic content, so the specificity gap widens as the concept saturates. |
| GEMMA-UNIFORM-DRAW-COLLECTED | **40/40, both degenerate classes empty** | Seed 42, `random.Random(42).sample(range(16384), 40)`, Python 3.12.11 stdlib MT. **Dead latents: 0/40. No-explanation: 0/40.** 30/40 carry both autointerp passes; 10/40 o4-mini only. Every drawn index retained — no redraws, skips, or substitutions. | The dead-latent denominator question is **moot for Gemma**: no exclusions, denominator = 40. The pre-registration still mattered — it was made before the data, which is the whole point. The browsed sample's one dead latent (idx 5000) was **luck of the browse, not a rate**. |
| BROWSE-BIAS-CONFIRMED-DIRECTIONALLY | **The two D1.2 negatives are withdrawn as browse artifacts** | The uniform draw surfaces topical/semantic domains the 33 browsed draws missed entirely — **12449 soil science, 15054 recipes, 11029 chemistry, 8024 horror themes, 13825 job postings, 14719 mechanical components**. | "No concrete physical object feature" and "no geography feature" were **bounded at n=33 and are now unsupported**: concrete-object-adjacent features appear immediately under uniform sampling. Withdraw both negatives rather than restate them with a caveat. This is also direct evidence that **the browse was the biasing step**, exactly as the PM argued — and it means the skew fraction may well move. Which is why pre-registration was done first. |
| D1.5-GATE | **CLOSED on what the gate was for — hook trustworthiness** | `d_model` 3840, `n_layers` 48, `HookedTransformer` over `Gemma3ForConditionalGeneration`, `blocks.31.hook_resid_post`, L0 65.61 vs 60.0 claimed, both paths local, zero network. **Steer leg is a positive causal control**: at 0.5–1.0× maxAct, feature 250 injects numbered-instruction structure into neutral prompts that is absent from baseline; 1.5× over-steers into repetition. Eyeball check found it fires on the **sentence-final period** of imperatives, not on instructional words — recorded, since a reader assuming verb-level firing misreads every activation plot. | The hook is demonstrably causally specific, which is what D1.5 was gating. **Do not launder the rest:** the ablation leg did not confirm weakening and is reopened separately below. |
| ABLATION-DESIGN-DEFECT | **Reopened — structural, not a prompt problem** | Two reruns failed in mirror image: the bicycle prompt's baseline was **imperative-saturated** ("here is a step-by-step guide" drives it independently of the feature); the laptop/recipe prompts' baselines showed **near-zero** concept presence (first-person past narrative, not second-person advice). Engineer 1's refinement: not only saturated baselines are uninformative — **near-zero ones are too, since there is nothing to weaken.** | **Generation-based ablation on generic prompts is structurally low-power** and cannot work for low-density features (12800 at 7.8e-4 will essentially never appear in a generic continuation). Running it as specified yields ~108 cells of uninterpretable data that reads as a result. **Fix: ablate on the feature's own top-activating text**, where by construction it is doing work — which dissolves the middle-range problem rather than tuning around it. Adds a judge-free necessity metric (ΔNLL / Δlogit under ablation), higher-powered than any generation comparison, and PI deliverable #1 is *feature-necessity ablation*, so this is the minimum to make it real, not scope creep. |
| EVIDENCE-DEPTH-MATCHED-39+1 | **Depth distributions are identical on both sides — a stronger match than either agent could see alone** | Qwen: `top_examples` already sorted descending (`characterize_lite.py:192`, `reverse=True`, verified empirically across all 40, zero exceptions), so truncation is a plain `[:16]` slice — **no re-sort**, which on ties would silently reorder and make the primary and depth-sensitivity arms disagree for reasons unrelated to depth. 39 features supply 16; **90863 supplies 2** (fired twice in 1,712,777 positions). Gemma: 39 features supply 16; **8667 supplies 2**. | Engineer 3 flagged 39/40 as "unavoidably asymmetric at exactly one," not knowing Gemma has the same shape. **Both sides are 39 × 16 + 1 × 2** — the depth distribution matches exactly, so the indeterminate-rate artifact is neutralised symmetrically. Coincidence, not design; the two thin features are different features in different models, and that must be said. **Diagnostic, now symmetric:** if either column's indeterminate rate lands one feature above the other, check 90863 (Qwen) and 8667 (Gemma) **before** concluding anything cross-model. Neither is dropped — removing a feature for being thin is precisely the content-based selection the seeded draw exists to prevent. |
| CONVERGENT-EVIDENCE-REFRAME | **PM ruling adopted — restructures the deliverable, not just the sentence** | Five unmatched axes (model, SAE architecture, expansion ratio, training provenance, relative depth) mean this is not a controlled comparison. PM: *"two independently-conducted measurements … converging on the same qualitative finding"* is the same facts and the stronger argument. | **Consequence the framing sentence alone does not cover: a side-by-side table with matched rows ASSERTS a controlled comparison in its layout, whatever the prose says.** Under convergent-evidence framing the deliverable becomes **two self-contained results with their own methods sections**, and the convergence is stated as a finding — not a `Qwen \| Gemma` column pair inviting row-wise subtraction. Same principle as field names travelling further than caveats, applied one level up to document structure. |
| QWEN-OVERDRAW-PROTOCOL | **Signed off — pre-registered over-recruitment** | Engineer 3 identified a genuine trilemma: dead-exclusion judged on *this* pass, a denominator of exactly 40, and one job cannot all hold. Resolution: 64 indices drawn in seed order, **all 64 measured in one pass**, taxonomy set = first 40 alive **in stream order**, reserves consumed strictly in seed order and only to backfill a dead primary. Rule written to `select_features.py` **before submission**. | Sound: reserve consumption is **deterministic and never content-based**, so the resulting 40 remains a uniform sample of *live* features — exactly the pre-registered "≥1 activating example" denominator. Expected discards ≈0.03 at `dead_fraction` 0.0008. **Added before data lands:** primary n=40 (matched to Gemma) **carries the claim regardless**; an n=64 within-Qwen stability check is pre-registered now as *sensitivity only* — it cannot be compared to Gemma, which has no reserves. |
| DEAD-LATENT-DENOMINATOR | **Pre-registered before the draw returns** | PM: choosing live-only vs all-drawn after seeing the dead rate is the same laundering one level lower. | **Two exclusions must not be conflated:** *dead* = no activations (a property of the SAE); *unexplained* = live but unlabelled (a property of Neuronpedia's coverage). Qwen has **no unexplained class at all** — `characterize_lite` generates its own snippets — so a live-only denominator would not be comparable. **Resolved: the headline denominator is "latents with ≥1 activating example" on both sides.** Adjudication reads *snippets*, not labels (snippet inspection already overturned both labels in 8 of 20), so an unlabelled-but-active Gemma latent stays in. The only exclusion on either side is zero activations. |
| DEAD-RATE-NOT-HARMONIZABLE | **Disclose and refuse to compare — harmonization declined with reason** | Qwen's dead determination: `characterize_lite`, 5,000 FineWeb docs, 1.7M positions, our threshold. Neuronpedia's: its own corpus and threshold. | Harmonizing would require re-running Neuronpedia's criterion over FineWeb — a GPU job behind the blocked transfer, impossible before deadline. So each side uses **its own evidence source's zero-activation criterion**, the two rates are reported **separately and never compared**, and the construct mismatch joins the divergence list beside corpus-max vs sample-max. Declining harmonization is the honest option, not the lazy one. |
| DEPTH-CONFOUND-UNDISCLOSED | **New — escalated to PM; not in the approved framing** | Engineer 2's records: Gemma layer **31/48 = 0.6458** depth fraction; Qwen `rwu04lpb` layer **28/48 = 0.5833**. Both models are 48-layer. Gap **0.0625** (3 layers). | The approved claim lists four axes — two models, two SAE architectures, two expansion ratios, two training provenances — and **omits relative depth**. SAE feature composition varies with depth, so this is a genuine fifth axis. Under existence-and-direction framing it is survivable, but it must be **named in the claim**, not discovered by a reviewer. |
| CLAIM-LIMIT-3v3 | **Binding limit, stated before the write-up** | The floor set's 3-semantic / 3-surface balance is n=3 per side. | Supports **per-feature reporting only**. If steering and ablation appear to differ across the semantic/surface split, it is reported as **suggestive with the n named** — never as a headline, never as a quantitative claim about differential behaviour between classes. Three versus three is an anecdote. |
| DOC-STALENESS-FAILURE-MODE | **Third distinct failure mode, named** | `docs/pi_directive_plan_2026_08.md` was **accurate but frozen at n=16** while adjudication had moved to n=20 — which is how the PM's 4500 grep came back empty. Distinct from the two content errors (13500 mislabelled, 13000 nonexistent). | **A stale document reads as authoritative** and is the more dangerous mode — same class as the stale checkout that produced three false readings earlier in the sprint, and as the line-number reuse across commits. Fix pattern: record the omission **inline** so the next reader sees why it was challenged, rather than silently correcting. |
| D3-QWEN-TAXONOMY-GAP | **Real gap — but the fix runs TODAY and is not Gemma-blocked** | Lab Assistant: no domain-class taxonomy exists for any Qwen SAE; only a hand-read top-30 survey and a 3-feature `characterize_lite`, neither with class labels or a denominator. The 58 % headline currently has **only the Gemma side**. | `scripts/characterize_lite.py` already did exactly this job — job 383755, **COMPLETED 14m32s**, 5,000 FineWeb docs → 1,712,777 positions on `rwu04lpb`, and it takes `--features` as a comma-separated list. Qwen weights and the FineWeb subset are **already local on `/scratch`**. Scaling 3 → ~40 features closes the deliverable's missing half **while Gemma is blocked**. Do not modify the script; it is read-only, writes no registry, and was built ad hoc for exactly this. |
| C5-QWEN-SAE-HEALTH | **False alarm — a defensible value exists** | `docs/implementation_log.md:154-159` **is** the A6 certificate table: `rwu04lpb` L28×32, job 383528, cert **`0a572198764d`**, verdict **amber**, fvu **0.0103**, ce_recovered **0.9884**, dead_fraction **0.0008**. Separately `:47` records training-time telemetry EV 0.9609 — a *different* measurement, superseded by the certificate. | The assistant reported "rwu04lpb has no A6." `docs/` is gitignored, so the Grep tool **silently skips it** — the same false-zero trap already recorded in memory. Cite the certificate, not the telemetry, and label the verdict **amber**, not green. |
| C4-CORRECTED-2026-08-07 | **My "~2.5× convergence is a finding" ruling is SCOPED DOWN — it described two atypical features** | Engineer 3's seeded n=40 gives the **unbiased** Qwen corpus-max distribution: range **3.86 → 60.50, median ≈13**. The anchors 9056 (47.50) and 47735 (40.75) sit at roughly the **95th percentile** — only 2 of 40 exceed 40.75. So `scale/maxAct` computed on them is arithmetic about two hand-picked strong features, **not a general dose anchor**; for a median feature the same absolute scale implies a multiplier ~3× higher. | The arithmetic was right and the generalisation was mine and wrong. Restated: **"~2.5× maxAct" is an observation on two 95th-percentile features, not a calibrated optimum.** Whether the optimum sits at a fixed *multiple* of maxAct or a fixed *absolute* activation is **exactly what the dose sweep tests** — it must not be assumed in the calibration. Gemma's grid {0.5, 1, 2, 4, 8, 16} spans both hypotheses (2.5× and ~9×), so it brackets either — **state that as luck, not design.** Calibrate against the seeded distribution, never the anchors. |
| C4-DOSE-AXIS-CONVERSION | ~~**Closes by arithmetic — no new compute**~~ **superseded by C4-CORRECTED above** | `docs/characterize_lite_findings.md` gives Qwen corpus maxima: **9056 (cheese) max 47.5**, **47735 (UNESCO) max 40.75**. Qwen's judged peaks — cheese @ scale 120, UNESCO @ scale 100 — convert to **≈2.53× and ≈2.45× maxAct**. | Both models land on a common x-axis after all, and the two Qwen optima agree at **~2.5× maxAct** — itself a finding. Gemma's grid {0.5, 1, 2, 4, 8, 16} brackets it well. **Caveat that must be stated:** Qwen's max is a *corpus* max over 1.7M positions; Gemma's `maxActApprox` is a *sample* max from Neuronpedia. Not the same construct — add to the declared-divergence list. |
| C3-TARGET-CONCEPT-SENSITIVITY | **No new ruling needed — it confirms the PM's scoping** | Two Lodestar runs over the **identical 104 generations** scored concept_relevance **9.50 vs 2.58** at scale 120, differing only in `"cheese"` vs `"cheese curds"` in the template — a ~3.7× swing from a wording choice. | Magnitude of judged relevance is not comparable across models; **direction against the random-feature control is** (control pinned at exactly 1.00 across all six scales). That is precisely the existence-and-direction-not-magnitude scoping already approved, so C3 strengthens it rather than reopening it. Binding: concept strings are **rule-derived from the adjudicated label, pre-registered before any judging**, identical protocol both models, no hand-tuning. Add a one-feature narrow-vs-broad sensitivity probe so the effect is **quantified** in the write-up, not merely disclosed. |
| D2-TRIM-ORDER | **Ratified as revised; step 2 pre-specified** | PM order: (1) drop 900 → 8; (2) drop two most domain-redundant → 6 **floor**; (3) only then doses → 4 points, both endpoints + two interior; (4) stop and escalate. Control is **permanently** off the table. | Step 2 resolved **now** so no 2am judgment call: drop **11000** (named entities/media titles — redundant with both 500 brands and 4500 person names) and **12800** (sports ordinals — redundant with 2048 dates, and lowest density at 0.000782, so the weakest dose calibration). Floor set = **250, 500, 2048, 2500, 3500, 4500** — six distinct classes, three surface-form (500, 2048, 4500) + three semantic (250, 2500, 3500). **Wording corrected 2026-08-07:** this floor does *not* carry "the skew claim's evidentiary base" — the skew fraction is measured by the **adjudication** samples (33 Gemma / 40 Qwen), never by the intervened set. The floor's balance matters for a **different** question: whether steering and ablation behave comparably on surface-form vs semantic features. **The 6 and the 33 are different populations answering different questions and must never share a denominator.** |
| MARKER-AS-PRIMARY-EVIDENCE | **Adopted 2026-08-08 with the direction disclosed against us — prereg v1.6 §11.1, `6194e13a…`, commit `20a3d3a`** | Both corpora carry per-token activations and neither adjudicator was using them on Gemma. Qwen has exact-span marking; Gemma's raw Neuronpedia JSON carries `tokens[]`, `values[]` and `maxValueTokenIndex` — present all along, unused because summarizer-mediated reads could not align parallel arrays. Demonstrated to be bucket-changing on a single row: **3070 reads as class 9 discourse-register from the window alone and class 2 lexical from the marker.** | **Classify on the trigger, not the context** — a feature is a function of activations, so what it fires *on* is what it detects. **This amendment favours our result, unlike class 11 which worked against it:** token-level triggers map to surface-form *by construction*. Therefore **two tallies are mandatory** — a primary, and a **conservative floor** where every marker-driven move *into* the numerator resets to `indeterminate`. If the finding does not survive the floor it is not reported as found. Pre-marker classes must be logged **at the moment they change**; they are unreconstructible afterwards. **Symmetry is the binding constraint, not the marker** — no row in either column may be classified under trigger-primacy until *both* columns have marker access (§8). |
| PARKED-VOIDS-THE-TALLY | **Binding 2026-08-08 — prereg v1.6 §11.2 — referred to PM for the deliverable-scope consequence only, not for the rule** | Lab Assistant B's own observation against their own throughput: parking selects **for** strong patterns, because strong patterns are what generate trigger-vs-context ambiguity. So the unparked residue is enriched for no-pattern rows, which are `indeterminate` by definition — B's last three unparked calls were all indeterminate. 11 rows currently parked (Qwen 14081, 33008; Gemma 819, 869, 1041, 1423, 2582, 2848, 7164, 7314, 8024). | **`parked` is a disposition, not a bucket; it never appears in a tally and must never be collapsed into `indeterminate`.** An unresolved parked row **voids the tally for its column** — it does not decay to the denominator, is not dropped, and the denominator is not reduced. This is the **sixth instance of the project's recurring directional artifact** (*anything that thins evidence deflates the surface-form count*) and **the first created by governance rather than by an instrument** — which is precisely why the rule has to bite against us. **Consequence referred to PM:** if the marker extraction fails, there is no Gemma composition table on the deadline — not a weaker one, none. |

## 2026-08-05 Both lanes accepted; publication is the single open action

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-V11-INTEGRATION-CANDIDATE-AUDIT | **Accepted for exact `664eda9`** | Seven blob hashes match — and each is the **same git blob object** at `a9a174f` and `664eda9`, so byte identity is structural. Nine single-parent commits, zero merges, one-to-one by `patch-id --stable` + subject. Scope checked two ways: endpoint diff **and** per-commit union, both exactly seven paths. R10 overlay byte-identical. 795/1/3 with the isolation test confirmed running. | **Repository readiness is complete.** Publication is the only remaining step. |
| R9-X5-PUBLISH-ACCEPTED-BUILDER | **Awaiting Researcher authorization** | Linear nine-commit replay on `origin/main` `9d90ef6`, zero merges — a push must create **no merge commit**. | Pre-push: re-verify the seven hashes and confirm `origin/main` is still `9d90ef6`. **If it moved, stop** — the R10-overlay-survival check is base-specific and the replay must be re-derived and re-audited. |
| R11-C00 + R11-C00-A | **Accepted (hash-bound)** | Four final SHA-256 values recorded in `VERIFICATION_LOG.md`. Second untracked-candidate acceptance under the R6-V5B precedent; `reports/` is gitignored so **the hashes are the only durable identity these files have**. | The PI-facing overclaim is closed. Spoken delivery is unblocked. |
| ENCODING-CRITERION | **Void and replaced** | "All four UTF-8 no-BOM" is wrong on two axes: the fiche has a genuine pre-existing BOM **and** is CRLF throughout, while the other three are LF. | Any future criterion is per-file **"encoding unchanged from pre-state."** Do not reintroduce the old wording. |
| R9-A7-CONSUMER-SURFACE-COMPLETENESS-RULE | **Complete — binding, no ED amendment** | Every consumer-read claim needs a named mechanism, declared class, and machine-proved path from a consumer entry point; the claim set is **derived from the manifests' exact-key sets**, never maintained by hand. Both are repository tests. | A5 failed at step (a) — noticing. The fix mirrors A4: convert "someone must notice" into "CI fails." |
| R9-C16-CONSUMER-COMPLETENESS-IMPLEMENTATION | **Blocked on publication** | Authorized now within existing paths: the mapping + totality + reachability tests (paths 1, 4); tooling cross-bind to `tooling_lock_artifacts()` plus setuptools/wheel checks (paths 1, 3); pip check re-run at `certification_environment_inputs` (path 1). | Sequenced after publication so the accepted candidate is not invalidated. **Mapping and reachability land first, before the new controls.** |
| R9-D5-EIGHTH-PATH-AUTHORIZATION | **Queued — Human / Researcher** | `schemas/environment_install_manifest/v1.schema.json` is **not** among the authorized seven and has `additionalProperties: false` throughout. Three items need it: tooling lock into `source_hashes`, `pip_check` attestation, `files_verified` attestation. | Escalated, not assumed. If authorized, the marginal cost of also rooting torch identity there drops sharply — **a Human election, not a self-authorized migration**. |

**New standing condition from A7:** the mapping and reachability test must land **before any
further ED-36 control is written**, not merely before first construction. Publication is
safe; growing the control surface without the test is not.

**Matrix rows still open:** 7, 10, 13 — routed to R9-V13. Rows 4, 6, 9 are NOT COVERED with
their fixes scoped into R9-C16 and R9-D5.

## 2026-08-05 Producer/consumer asymmetry is a class — stop point-fixing

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| MATRIX-ROW-4-TOOLING-CROSS-BINDING | **NOT COVERED — V9B-F1 MAJOR** | `_validated_tooling_lock_files` (`:775`) does real recomputation but is reachable only from `validate_bundle`; the cert-lane gate `_validate_tooling_entries` (`:3263`) binds to `manifest["generator"]` and never references the lock. Self-consistent `9.9.9` admitted. `setuptools`/`wheel` have **no version check at all**, despite R9-D2 ratifying exact identities. | Second manifest-binds-to-itself defect after V9-F1. |
| MATRIX-ROW-9-PIP-CHECK | **NOT COVERED — V9B-F2 MAJOR** | The check is unconditional, `check=True`, fail-closed, and precedes the manifest write — R9-A1 holds. But no `pip_check` field exists anywhere in the module or install schema, so the consumer can neither verify nor re-run it. | Control-surface defect, not a live exploit — but it is the "assume the producer ran" reasoning A5 forbids. |
| V9B-F3-PRIVATE-PIP-ASSERT-COVERAGE | **Tracked — test-only, no ruling needed** | `_assert_bootstrapped_pip_only` (`:1529`) has four `raise` branches and two R9-D2 stop conditions; all five test references stub it with `lambda: None`. Zero tests exercise it. R9-V2's original finding stands. | Deferred until after publication so the repository-readiness lane reaches `origin/main` without further churn. |
| R9-A7-CONSUMER-SURFACE-COMPLETENESS-RULE | **Active — Architect** | Four independently-written controls, same shape: TL (V8), torch (V9-F1), tooling (V9B-F1), pip check (V9B-F2). | A rule the Engineer applies **once**, not four point fixes. |

**Matrix: 7 covered by ledger count** (1, 2, 3, 5, 8, 11, 12) / **6 by the Auditor's
stricter count** — the difference is row 2, covered by Orchestrator transcription of the
Auditor's own ancestry proof rather than a dedicated audit. No further work either way.
Remaining: **row 7**, then 13, 10.

**Repository-readiness lane is unaffected** and stays clean: `664eda9` under audit at
R9-V11, then publication. All findings above belong to construction readiness.

## 2026-08-05 Packaging question closed; integration candidate `664eda9` ready for audit

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-X3-TAMIA-PACKAGING-PROBE | **Complete — not a bootstrap break** | Tamia login node, exit 0: `packaging` 23.1 resolves to real `site-packages`, **not** `pip/_vendor`; all four symbols import; `sys_tags()` returns 39; `tomllib` confirms a genuine 3.11.5 interpreter. | R9-C1 did not break the cluster. Benign **by circumstance, not by guarantee**. |
| R9-A4-CRITERION-REWORDING | **Done** | "stdlib-only" withdrawn as false. Binding phrasing recorded in `VERIFICATION_LOG.md`: the bootstrap **depends on `packaging` ≥ 20.9 in the pre-activation system environment — a documented external assumption**. `jsonschema` must still never appear pre-activation. | Honest description replaces a claim the code never satisfied. |
| R9-SYS-TAGS-ENVIRONMENT-SENSITIVITY | **Tracked — construction readiness** | `compatible_tags` uses ordered `sys_tags()` compared with list `!=` (R9-V3). Tamia: 39 tags (py3.11.5 / packaging 23.1). An Orchestrator control gave 42 (py3.12.11 / packaging 26.2) — the **wrong comparison pair**, proving only environment-sensitivity, not a mismatch. | When a real build host exists, run the probe in `python:3.11-slim-bookworm` and compare against **39**. A difference fails closed at first construction. |
| R9-I1-BUILDER-ONTO-PUBLISHED-MAIN | **Engineer complete — candidate `664eda9`** | Base `9d90ef6`; merge-base exactly `4bf0fd8`; nine commits cherry-picked cleanly with **zero conflicts**, one-to-one chain; all seven blob hashes match R9-V10; diff exactly seven paths; R10 blobs byte-identical; 795 passed / 1 skipped / 3 deselected; `bash -n` on all seven launchers. | Route to independent integration audit; **no push**. |
| R9-C15-PREFLIGHT-DEPENDENCY-DIAGNOSTIC | **Deferred until after publication** | A fail-fast preflight message naming a missing `packaging` beats an opaque `ImportError` at `:33`, plus an explicit ≥ 20.9 floor. | Touches the seven paths; doing it now would invalidate the accepted integration candidate. |

**Repo-local `core.autocrlf=false` retained deliberately.** Set unintentionally by a failed
`cd`, but it is the value every worktree has used since the R8-I2 CRLF incident and makes
LF-faithful checkout the default. Verified: local `false`, global unset. Recorded so it is
not later mistaken for drift.

**Line-number discipline, now explicit after two Orchestrator errors** (5131 vs 5148;
`:1381` vs `:1657`): line numbers and counts must be re-derived per commit, never carried
across one.

## 2026-08-05 R9 builder ACCEPTED end-to-end; integration unblocked

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-V10-COMBINED-INTEGRATION-AUDIT | **Accepted for exact `a9a174f`** | Fresh independent Auditor; seven blob hashes recorded and **Orchestrator-re-verified at log time**; all eight priorities pass; pre-activation isolation proved by execution **with a negative control**; drift corpus proven able to fail; 795 passed / 1 skipped / 3 deselected; static gates clean; worktree state fully accounted. | **R9-I1 integration is unblocked.** The ED-36 builder is repository-accepted. |
| R9-I1-BUILDER-ONTO-PUBLISHED-MAIN | **Active — Engineer 2** | Base `origin/main` = `9d90ef6`; R9 chain forks from `4bf0fd8`; overlays path-disjoint (R9 = seven builder paths; R10 = `slurm/launch_census.sh` + a T1.2 doc). | Acceptance criterion is byte-identity of the seven paths against the recorded hashes. |
| R9-C14-PRIVATE-PIP-IDENTITY-COVERAGE | **Tracked — non-blocking, pre-existing** | Assertions at `environment_bundle.py:1749-1750` are uncovered; neutralising both yields 147 passed / 0 failed. Predates the candidate. R9-C10's narrowing to success-only is correct and necessary — a check that may itself raise cannot run while preserving a primary error — and fails closed. | Add a positive test so a refactor cannot silently drop them. |

**Construction-readiness lane, unchanged and still open:** matrix rows 4, 6, 7, 9, 10, 13;
V9-F1 torch authenticity (R9-X4 attestation → R9-C13); the `packaging` pre-activation fact
(R9-X3 dispatched); R9-C12 consumer-only adversarial matrix. **Integration and publication
do not confer construction authority.**

## 2026-08-05 R9-X3 authorized; probe design corrected before dispatch

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-X3-TAMIA-PACKAGING-PROBE | **Authorized — Lab Assistant, login node** | Human-authorized single read-only diagnostic. The whole-node GPU rule governs **jobs**; this has no allocation, no scheduler, no tensor math. Decisive reason it must *not* go through `sbatch`: `setup_env.sh` runs on the login node and creates the venv the launchers later activate inside their sbatch payload, so **the login-node pre-activation state is precisely the environment under test** — a compute-node pass would prove nothing and would spend an H100 allocation on a non-answer. | Returns raw JSON only; interpretation belongs to R9-A4. |
| R9-X3 probe design | **Corrected by the Researcher before dispatch** | The Orchestrator's proposed `import packaging` one-liner could return a **false benign**. Orchestrator-verified at `:33-36`: the module imports four symbols across three submodules — `packaging.tags as packaging_tags` (3 uses), `packaging.requirements.Requirement`, `packaging.utils.canonicalize_name`, `packaging.utils.parse_wheel_filename` (2 uses). `parse_wheel_filename` exists only from packaging ≥ 20.9, and `tags.sys_tags()` is load-bearing for the V3-confirmed SOABI/ordered-tag verification. A bare import can succeed against a partial, vendored, or old install that still dies at `:36`. | The replacement block probes each symbol separately, plus `packaging.__file__` (to catch `pip/_vendor` shadowing), `__version__`, `sys_tags()` count, and `tomllib` (3.11+ stdlib, confirming the interpreter actually resolved to ≥3.11). |
| R9-A4-CRITERION-REWORDING | **Required regardless of probe outcome** | "stdlib-only" is already false as a description of `environment_bundle.py`, independent of whether the cluster satisfies the dependency. | If the probe passes: the honest phrasing is that the bootstrap **depends on `packaging` being present in the pre-activation system environment — a documented external assumption, not a guarantee**. If it fails: hard R9-C1 bootstrap break, and R9-C9's by-name exemption needs revisiting. |

## 2026-08-05 R9-A6 ruled; torch closure is a Human attestation, then an Engineer item

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-A6-TORCH-AUTHENTICITY-ROOT | **Complete — binding ruling, no ED amendment** | Commit the sanctioned identity at a pinned revision; enforce by consumer-side recomputation. `import_alliance_torch_artifact` retained as capture instrument, not anchor. Option (a) rejected: manifest self-certification means a substitute bundle brings a substitute manifest and the chain protects the wrong value — **self-reference is not a root**. | Closure path is settled. |
| R9-X4-ALLIANCE-TORCH-ATTESTATION | **Queued — Human / Researcher, before first construction** | No cryptographic root exists; the Alliance wheelhouse publishes no hash the lab controls. Root must be a named human attestation — attester, timestamp, source host, source path, capture method — beside measured sha256, size_bytes, filename. Grounded in ED-8 (checksum as operative identity when the recipe is unrecoverable) and ED-30 (value travels with source and confidence). | Requires cluster access to obtain the artifact. Not needed for integration. |
| R9-C13-TORCH-AUTHENTICITY-ROOT | **Blocked on R9-X4** | Renamed from the Architect's `R9-C5`, which collides with the pre-recorded derived-wheel export gate. | Minimal form needs no eighth path: constants beside `_ALLIANCE_TORCH_VERSION` in path 1, bound by `repo_revision`. A separate committed data file would need an eighth path — escalate, do not self-expand. |
| A5-CLASSIFICATION-REFINEMENT | **Adopted** | **An attestable-only fact becomes recomputable by being committed at a pinned revision.** Two stages, two classes. | Prevents a future Engineer concluding that "attestable-only" means the consumer cannot enforce. Applies beyond torch. |
| ORIGIN-PREFIX-CHECK-LIMITATION | **Binding** | `_ALLIANCE_TORCH_ORIGIN_PREFIX` matching proves only that the manifest author typed a conforming string. It proves nothing about the bytes. | **Labelling-consistency check, not authenticity.** Must say so in its own documentation and may never be cited as authenticity evidence in any manifest, report, run card, or audit finding. |

**Process defect, third occurrence:** Architects have now proposed three colliding work-item
IDs (`R9-C3`, `R9-C4`, `R9-C5`). Future Architect briefs must carry a pre-assigned ID.

## 2026-08-05 R9 branch tip stable; integration audit is the gate

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-C10-ROLLBACK-AND-TL-COVERAGE | **Engineer complete — `a9a174f`** | Three unsafe rollback sites fixed across three functions (`_extract_sdist_to_directory` ×2, `_bootstrap_private_pip` nested-`finally` shape, `build_runtime_bundle`); `_build_derived_runtime_wheel` found already safe and correctly left unchanged. V8-F1 closed with all three TL negatives. 795 passed. | Last bounded item. Tip stable. |
| R9-C8 scope claim | **Corrected** | R9-C8 reported four functions carrying the unsafe pattern; only three did. The Engineer self-corrected in the C10 report. | The C10 per-function table is the durable record; do not re-cite the C8 four-function claim. |
| V8-F1-TL-BRANCH-COVERAGE | **Closed** | Branches 2 and 3 plus a consumer-path negative. The consumer negative shows `3.4.0` is caught by a **generic** version check with no TL-specific wording — empirically corroborating R9-V8's static finding that the TL gate never reaches the consumer. | Row 11 coverage defect closed. |
| R9-V10-COMBINED-INTEGRATION-AUDIT | **Active — Auditor 1 (fresh, major milestone)** | Delta `1aca37d..a9a174f` = three commits, 4 files, +827/−17; cumulative from `4bf0fd8` still exactly seven paths. | Independence: Auditor 2 has audited V5/V6/V8/V9/V9B on this branch, including its own predecessor commits. A fresh auditor is proportionate before integration and publication. |

**Not gated by this audit** (construction-readiness lane): matrix rows 4, 6, 7, 9, 10,
13; V9-F1 torch authenticity (R9-A6 with the Architect); the `packaging` pre-activation
fact; and R9-C12's consumer-only adversarial matrix.

## 2026-08-05 R9-V9: first real hole found; construction readiness separates from repository readiness

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-PACKAGING-PREACTIVATION-PROBE | **Confirmed breaking; one fact outstanding** | With `packaging` blocked, `preflight` returns rc 1 and writes no plan, dying at `:33` before subcommand logic; `create-venv` identical. Lazy import cannot fix it — `packaging_tags.sys_tags` is genuinely called at `:246` on the preflight path. Introduced by **R9-C1 (`c847e07`)**, not pre-existing. | Needs one read-only Tamia login-node line to decide benign-by-luck vs hard cluster bootstrap break. Then a scope ruling, not a mechanical edit. |
| MATRIX-ROW-6-ALLIANCE-TORCH | **NOT COVERED — V9-F1 MAJOR** | 15 manifest/artifact and 9 live-runtime attacks all correctly rejected, but two wheels with different bytes sharing `2.13.0+computecanada`, correct METADATA, and an `alliance:wheelhouse` origin are **both admitted**. No SHA constant; torch absent from the source-hash-bound export; `import_alliance_torch_artifact` is CLI-only (`:5118` in `main`), unreachable from any consumer. | The first genuine hole in this campaign, not a coverage gap or layering asymmetry. **The hash binds the manifest to itself.** |
| R9-A6-TORCH-AUTHENTICITY-ROOT | **Active — Architect** | A5 follow-through: torch is the second control in A5's class, and unlike TL no stronger consumer mechanism compensates. | Rule the closure: hash-bind the receipt into the manifest, or commit the sanctioned identity so it is source-hash-bound like the export. |
| R9-V9B-MATRIX-ROWS-4-AND-9 | **Active — Auditor 2** | Rows 4 (tooling cross-binding) and 9 (pip check) remain first-time work at frozen `1aca37d`. | Needed before the release audit; disjoint from Engineer 2's live branch. |

**Strategic separation, now explicit.** R9-A5 ruled that none of this gates R9-I1:
integration publishes producer-side code that adds a layer and removes nothing.
**Repository readiness** (accepted branch → integrate → publish) and **construction
readiness** (matrix rows 4/6/9/10/13, V9-F1, the packaging fact, and the R9-C12
consumer-only adversarial matrix) are now tracked as separate gates. Do not let
construction-readiness findings block integration, and do not let integration imply
construction authority.

## 2026-08-05 R9-C9 landed; R9-A5 rules the consumer surface

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-C9-SCHEMA-ENFORCEMENT-BOUNDARY | **Engineer complete — `92a12f6`** | Paths 1/4/7; schema `required` arrays tightened to match the stricter Python validators (direction correct, no validator touched); blocking-import-hook proof by execution; 9-case anti-drift corpus with semantic-only cases documented as expected schema-acceptance; 787 passed. | Closes R9-SCHEMA-ENFORCEMENT-GAP pending audit. |
| R9-A5-CONSUMER-SURFACE-CONTROL-ASYMMETRY | **Complete — binding ruling, no ED amendment** | Producer checks are fail-fast ergonomics; consumer checks are the security boundary. Three-class rule (recomputable / attestable-only / declarative) plus the hand-crafted-bundle test. TL is a layering asymmetry, not a hole. | **Does not gate R9-I1.** **Does gate first real construction** behind a consumer-only adversarial matrix. |
| R9-C12-CONSUMER-SURFACE-ADVERSARIAL-MATRIX | **Queued — after R9-I1, before first construction** | Renamed from the Architect's `R9-C4`, which collides with commit `1ed3ad9`. | Co-testing producer and consumer cannot prove consumer enforcement; inputs must be hand-crafted. |
| R9-PACKAGING-PREACTIVATION-PROBE | **Active — Auditor 2** | `packaging` is a module-level non-stdlib import at `92a12f6`; the blocking-import hook covers the `jsonschema` closure but not `packaging`. Pre-activation import on Tamia has never been exercised — cluster preflight has never run to completion. | Potential latent bootstrap break predating all R9 work. Diagnostic only. |
| R9-C10-ROLLBACK-AND-TL-COVERAGE | **Active — Engineer 2** | Combines R9-C11 (unsafe `except: _rollback…; raise` in four construction-path functions) and V8-F1 (TL branches 2 and 3, five of eight call sites, consumer-path rejection — all zero tests). | One commit; the combined C8+C9+C10 tip is then audited once. |

**Matrix: 7 of 13 covered.** Row 7 pending audit of `70c6d25`. Remaining uncovered:
4, 6, 9, 10, 13. Release audit still **not queueable**.

**Standing hazard — third occurrence.** R9-A5's Architect read `D:\qwen-sae-interp` at
stale main `c6ef2df` (2279 lines) rather than the R9 tip (5112/5131). Earlier
occurrences: Engineer 1's R11-C00 worktree, and the R12-C1 worktree trap. **Every role
brief must name the exact revision and worktree to read.**

## 2026-08-05 R9-A4 ruled; matrix now 7 of 13 covered

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-A4-SCHEMA-ENFORCEMENT-BOUNDARY | **Complete — binding ruling, no ED amendment** | Python validators are the normative contract on all paths; schema is declarative shape, lazily enforced only at the two post-activation sites; the pre-first-write condition closes via a directional anti-drift CI test (validators ≥ schema, never bidirectional). Paths 1, 4, 7 only. | R9-C9 implements. Halt if `_schema_registry.py` or `config_lifecycle.py` must change. |
| R9-C9-SCHEMA-ENFORCEMENT-BOUNDARY | **Active — Engineer 2** | Renamed from the Architect's `R9-C3`, which collides with commit `490ae73`. | Closes R9-SCHEMA-ENFORCEMENT-GAP and unblocks R9-I1. |
| R9-C8-FINALIZATION-ROLLBACK-DEFECT | **Complete — reproduction negative, commit `70c6d25`** | `finalize_bundle` routes through `_rethrow_primary_with_cleanup` (`:384-393`) and preserves the primary error; the regression passed against unmodified `1aca37d`. Test-only, 40 insertions, 773 passed / 1 skipped / 3 deselected, static gates clean. | Matrix row 7 was a **coverage gap, not a live bug**. Row 7 pending independent audit of `70c6d25`. |
| R9-V8-TL-SEPARATION-FIRST-AUDIT | **Row 11 COVERED** | Eight enforcement sites, fails closed on every constructed contamination, no ordering bypass, second source of truth via the source-hash-bound export pin. | Highest-priority uncovered row is closed. |
| V8-F1-TL-BRANCH-COVERAGE | **Queued — Engineer, test-only** | All 11 TL tests exercise branch 1 only; branches 2 and 3, five of eight call sites, and all consumer-path TL rejection have zero tests. Same class as V3's F1, ruled BLOCKING by R9-D4 §4. | Three deterministic negatives; no production-code change. |
| R9-C11-UNSAFE-ROLLBACK-PATTERN | **Queued — Engineer** | `except Exception: _rollback_partial_path(x); raise` in `_extract_sdist_to_directory`, `_bootstrap_private_pip`, `_build_derived_runtime_wheel`, `build_runtime_bundle` — the bare `raise` never executes if cleanup throws. | These sit on the real bundle-construction path, where a masked primary error is most costly. Collides with R9-C9 on path 1; serialize. |
| R9-A5-CONSUMER-SURFACE-CONTROL-ASYMMETRY | **Active — Architect** | Two independent ED-36 controls are absent from the consumer surface: the TL gate is producer-side only (unreachable from all six consumer functions) and the acquisition schema reaches no production read path. | Pattern, not point fixes. Architecture question before the release audit. |

**Matrix status: 7 of 13 covered** (1, 2, 3, 5, 8, 11, 12). Remaining uncovered:
4 (tooling cross-binding), 6 (Alliance torch), 9 (pip check), 10 (non-overwrite,
re-anchor only), 13 (retained state). Row 7 pending audit. Release audit remains
**not queueable**.

**Withdrawn:** the `rpds-py` offline-wheel watch item recorded 2026-08-04 was wrong
and must not be re-cited. See `VERIFICATION_LOG.md` for the verified export lines.

## 2026-08-05 R9 end-to-end certification gap; release audit not queueable

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-V1-PREP-MATRIX-ABSENCE | **Reconstructed — 8 of 13 rows uncovered** | Row set recovered from the durable enumeration at `CURRENT_PLAN.md:1387`; per-row criteria are a **lossy** reconstruction from V1/V2 failing-boundary lists. Rows 1, 2, 3, 5, 8, 12 covered (row 2 by Orchestrator ancestry transcription); rows 4, 6, 7, 9, 10, 11, 13 not. | Supersedes the earlier "reconstruct into durable memory" action. |
| R9-AUTHORITATIVE-PATH-SET | **Ruled — V5/V6 set governs** | C1 changed five; D3 added `test_slurm_setup_env.py`; D4 added the acquisition schema. `environment_install_manifest/v1.schema.json` was never modified on the branch. V3's enumeration held two transcription errors and is not a competing authority. | Closes the question R9-V3 raised and never had answered. Row 1 certified. |
| R9-A4-SCHEMA-ENFORCEMENT-BOOTSTRAP-BOUNDARY | **Active — Architect** | `load_acquisition_manifest`/`validate_acquisition_manifest` is one shared body serving both pre-activation (`preflight`, `create-venv`) and post-activation (`record-installed`, `certification_environment_inputs`) callers. Lazy `jsonschema` import breaks the cluster bootstrap; partial enforcement leaves `preflight` — the install gate — unenforced. | Genuine ambiguity. R9-C7 correctly did not implement. |
| R9-C8-FINALIZATION-ROLLBACK-DEFECT | **Assigned — Engineer 2** | Matrix row 7 carries the only unclosed concrete defect in the set: R9-V1's Windows `PermissionError` masking the intended environment error, never recorded closed across four commits. | Independent of R9-A4; dispatched in parallel. |
| R9-V8-TL-SEPARATION-FIRST-AUDIT | **Assigned — Auditor 2** | Matrix row 11, highest priority uncovered: the TransformerLens 3.2.1 contamination boundary. Code present at `1aca37d` (`:457`, `:2075`); R9-V2 recorded all TL surfaces untested. | Scientific contamination boundary; first-time audit, not a re-check. |
| RELEASE-AUDIT | **Not queueable** | No single audit has certified the builder end-to-end. Rows 4, 6, 7, 9, 11 are first-time work. | Do not schedule the final release audit until those close. |

**Unchanged:** R9-SCHEMA-ENFORCEMENT-GAP remains a pre-first-write blocking
condition. R9-I1 integration stays queued behind the R9-A4 resolution.

## 2026-08-04 R9-V6 accepted; schema enforcement assigned before integration

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-C6-ED36-ATTEMPT-SHAPE-HARDENING | **Accepted at `1aca37d`** | R9-V6 delta audit; seven blob hashes in `VERIFICATION_LOG.md`. | ED-36 builder hardening is complete. |
| R9-ISOLATION-TEST-SKIPS-ON-HOST | **Closed** | `tests/…builder.py:1381` passed in-container as a real pass, with a falsification control proving the assertion is carried by the `_deny` guard rather than by `--network none`. | The builder isolation boundary is not reopened. Integration is not blocked by it. |
| R9-C7-ED36-ACQUISITION-SCHEMA-ENFORCEMENT | **Assigned — Engineer 2, before integration** | `interplab/core/_schema_registry.py` provides `validate`, already consumed by `envelope.py`, `configs.py`, `battery.py`, and `jobs/validate.py`. Enforcement therefore fits **path 1 + path 6** of the existing seven — **no eighth path, no scope ratification needed**. `battery.py` is the precedent for validating a non-artifact, non-config schema via `SCHEMAS_ROOT`. | Sequenced before R9-I1 so the branch integrates once and the seven-path set stays the unit of acceptance. |
| R9-C7 CONSTRAINT — stdlib-only bootstrap | **Escalation trigger** | `environment_bundle.py` imports **only stdlib** at `1aca37d`. R6-C1 established a "stdlib-only outside-repo bootstrap" for this module, so the import list is deliberate. Forcing `jsonschema` into that path would break the bootstrap contract — and `jsonschema` pulls `rpds-py`, which R9-V6 measured as needing compilation with no offline Linux wheel. | Enforce only on a consumer/read path that runs inside a provisioned environment. If the read path and the bootstrap path are the same, **STOP and escalate to the Architect (R9-A4)** rather than implementing. |
| R9-I1-BUILDER-ONTO-PUBLISHED-MAIN | **Queued — after R9-C7** | Base `9d90ef6`; R9 chain forks from `4bf0fd8`; overlays path-disjoint. | No push; publication is a separate authorization. |

**Ruling on sequencing.** Integration itself is inert with respect to the schema gap
— it constructs no bundle and writes no manifest. But R9-C7 needs no new path
authority, so doing it first costs one cycle and avoids a second integration. The
pre-first-write blocking condition remains binding regardless: **no real acquisition
manifest may be written until the schema is enforced on a production read path.**

## 2026-08-04 R9-C6 complete; one residual boundary gap named

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-C6-ED36-ATTEMPT-SHAPE-HARDENING | **Engineer complete — commit `1aca37d8e437125e70ba793a4882b5aac9760f66`** | Orchestrator-verified: delta from `82b028e` is exactly 2 files (+39/−3), cumulative from `4bf0fd8` is still exactly the seven authorized paths, repair worktree clean. Ternary parenthesised and confirmed in the committed bytes at line 4269. 772 passed (770 + 2 new negatives), 1 skipped, 3 deselected. Exact CI Ruff, `uv lock --check`, `bash -n` ×6, `git diff --check` all clean. | Route to R9-V6 delta audit. |
| R9-ISOLATION-TEST-SKIPS-ON-HOST | **Residual gap — close at R9-V6** | The skipped test is named: `tests/test_environment_bundle_builder.py:1381`, "the direct child-source boundary requires a Linux /proc namespace host." It was already skipping at R9-V5 (770 passed, 1 skipped). V5 proved namespace isolation by **manual** Docker probe; the **repository regression guarding that boundary has never executed**. | Same class as R6's exact-byte gap, which stayed `UNVERIFIED` until a real test ran. R9-V6 must execute this test inside the proven Linux container to convert the skip into a pass. Not a reopening of V5. |

**Line-number drift, recorded:** the ternary is at `:4269`, not `:4266` as carried in
the V5 advisory and the Orchestrator work order — a one-line drift introduced by the
R9-V4 commit. The Engineer verified the actual location rather than trusting the
prompt. No action beyond this record.

**Evidence-independence note:** the Engineer corroborated the four do-not-touch
hashes against `project_management/VERIFICATION_LOG.md`. That is **not** independent
corroboration — the work order and the log share one source, the V5 audit record.
The independent check is the Auditor's own re-hash at R9-V6.

## 2026-08-04 A-005 closed — tiered tracking by file type

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| A-005-REPORT-RESULT-TRACKING | **Closed by Researcher ruling** | Split by file type, not directory. The `project_management/` analogy does not transfer: A-003 is noise management, whereas `reports/` holds the deliverable and `results/` the evidence it cites by path. Tier 1 (track now): 11 `.md`, 3 `.py`, 18 `.csv`, 9 `run.json` — under 0.5 MB excluding `run.json`. Tier 2 (defer to release audit): 5 `.pptx`, 49 `.png`. Rationale: git cost comes from rewriting, not size — `run.json` is write-once (19.8 MB → ~3–5 MB once), while a `.pptx` is ~8 MB rewritten wholesale per revision. | `project_management/` at `.gitignore:46` is unchanged; A-003 stands. Binary durability goes to the release audit as a costed LFS-vs-external-backup decision. |
| R12-C1-A005-TRACK-REPORT-DELIVERABLES | **Queued — Engineer 1, blocked on R11-V1** | Type-scoped rules replace the blanket ignores at `.gitignore:19` (`results/`) and `:45` (`reports/`), both line numbers confirmed. | See the two sequencing constraints below — this must not run concurrently with R11-C00. |
| R12-X1-PUBLISH-TRACKED-DELIVERABLES | **Blocked — requires explicit authorization** | Committing `reports/` and `results/` publishes internship-report prose and run evidence to the shared remote Tamia consumes, for the first time. | External-disclosure gate. Authorization must name the exact commit and branch. |

### Orchestrator verification of the R12-C1 premises

- `.gitignore:19` = `results/`, `.gitignore:45` = `reports/`, `:46` = `project_management/`. Confirmed.
- Counts confirmed exactly as predicted: 11 `.md`, 3 `.py`, 18 `.csv`, 9 `run.json`; excluded 5 `.pptx`, 49 `.png`.
- `results/` holds **218 files**. The pattern admits exactly 27 (18 `.csv` + 9 `run.json`) and correctly excludes 64 `.whl`, **59 non-`run.json` `.json`**, 21 `.png`, 10 `.html`, 9 `.sqlite`, 7 `.md`, 7 `.txt`, 6 `.log`, 1 `.msgpack`.
- **CI Ruff does not cover `reports/`.** `ci.yml:28` targets `interplab tests scripts/*.py data/concepts/*.py`. Tracking the three report `.py` files therefore causes **no CI regression** — but they carry **189 Ruff errors** (`build_progress_slides.py` 107, `build_interlab_governance_slides.py` 80, `diagnose_pptx_open.py` 2). Recorded as a known unlinted pocket: any future widening of the Ruff target to `.` turns CI red.
- The third `.py` is `diagnose_pptx_open.py` (1 KB, 2026-07-26) — a one-off "truncated variants for PowerPoint-open bisection" diagnostic, not a deliverable builder. The ruling's rationale covers the two real builders (45 KB, 23 KB). Tracking it anyway is recommended: a per-file exception would complicate a rule whose simplicity is its virtue.

### Two binding sequencing constraints

1. **R12-C1 must not run before R11-V1 accepts.** R11-C00 is editing four files under `reports/` in place right now, under an acceptance criterion of "`git status --porcelain` unchanged, proving nothing entered the index." Changing `.gitignore` mid-flight un-ignores those files, breaks that criterion for reasons unrelated to the Engineer's work, and places two writers in one file scope.
2. **Ordering also protects the content.** If R12-C1 landed first, the first-ever tracked version of `internship_report.md` would carry the exact overclaiming prose R11-D1A just ratified a correction for — published to a shared remote. Correct first, then track, so the initial tracked version is the corrected one.

### Worktree trap (second occurrence)

A worktree cut from `origin/main` contains **no** `reports/` or `results/` — `git worktree` materializes tracked content only. Ruling: fast-forward local `main` (`c6ef2df` → `9d90ef6`, ff-only; the tracked tree is clean and untracked files survive a fast-forward) and perform R12-C1 in the main worktree where the files actually live. This avoids a ~20 MB copy plus hash-verification step and clears a standing wart.

## 2026-08-04 R9 builder accepted

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-V3-ED36-BUILDER-REACCEPTANCE | **Verdict recovered — REJECT on `1ed3ad9`** | F1–F4 transcribed into `VERIFICATION_LOG.md` from the Auditor's own record at `d:\lodstar\R9_V3_...AUDIT.md`, closing the durable-memory gap opened on 2026-08-04. | Documentation defect closed. |
| R9-V5-ED36-BUILDER-FINAL-ACCEPTANCE | **Accepted for exact `82b028e`** | Seven governing blob hashes recorded. All four V3 findings independently re-derived as closed; nine branch-unique negatives with an accepted clean baseline; real Linux namespace evidence executed offline; 770 passed/1 skipped/3 deselected; builder construction byte-unchanged; 14 worktrees at start and end, tip unmoved. | The ED-36 bundle builder is independently accepted. This is repository acceptance only — it authorizes **no** target capture, bundle construction, Tamia environment mutation, equivalence, certification, or experiment. |
| R9-C6-ED36-ATTEMPT-SHAPE-HARDENING | **Queued — Engineer 2** | Auditor advisory: `_validate_derived_entry_shape` applies only `_require_mapping` to both connection attempts (`:4313-4314`); ternary at `:4266` unparenthesised (precedence proven correct behaviourally). | Land before integration so the branch is integrated once. |
| R9-SCHEMA-ENFORCEMENT-GAP | **Pre-first-write blocking condition** | The acquisition schema is not applied on any production path; `environment_bundle.py` never imports the schema registry and no job validates the manifest against it. Python validators are the sole runtime gate. | Non-blocking for accepting `82b028e`; **blocking before the first real acquisition manifest is written**. Schema path seven was ratified under R9-D4 to encode the trust chain. |
| R9-V1-PREP-MATRIX-ABSENCE | **Release-audit prerequisite** | The matrix exists only as `COMPLETION_LEDGER.md:227`; no document was ever written. Same defect class as the missing R9-V3 verdict — audit artifacts living in conversations. | Reconstruct into durable memory and certify V3+V5 coverage row by row **before the final release audit**. Not a gate on R9 now: V5's item list plus V3's certified-adequate set is the governing acceptance. |

## 2026-08-04 R11-C00 execution-surface ruling (reports/ is untracked)

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R11-C00-EXECUTION-SURFACE | **Ruled — edit in place, no commit** | Engineer 1 stopped before editing. Independently confirmed: `.gitignore:45` excludes `reports/` under the header "Personal / local docs (not for public repo)" (same block as `docs/` and `project_management/`); `git ls-files reports/` = 0; `git log --all -- reports/` = 0 commits on any ref including `origin/main`. The four target files exist only as untracked files in `D:\qwen-sae-interp`, last modified 2026-07-26/27. | The prior "fresh worktree from `origin/main` + one clean commit" instruction was void for this lane — `git worktree` materializes tracked content only, so the cut worktree was correctly empty of `reports/`. Orchestrator instruction defect, not an Engineer error. |
| A-005-REPORT-RESULT-TRACKING | **Open — must NOT be resolved as a side effect** | Force-tracking via `git add -f` would extend version control to files deliberately excluded, and would publish internship-report prose to the shared remote Tamia consumes. That is the open A-005 question plus an external-disclosure decision. | Refused as an implementation action. Queued as a separate Researcher decision (R12-D1); it does not block R11-C00. |
| R11-C00 evidence binding | **Hash-bound, per R6-V5B precedent** | R6-V5B accepted an exact local candidate bound to file SHA-256s (`740dd611…`, `7bbc1152…`) while both files were untracked. Auditability does not require a commit. | Pre-edit immutable snapshot + SHA-256 replaces the commit SHA as the audit anchor. `git diff --stat` is void for this lane and is replaced by a snapshot-derived diff. |

**Durability risk on record:** because `reports/` is ignored, this correction is
not reproducible from a clean clone and will be absent from any regenerated
checkout. The corrected prose exists in exactly one place on one machine. A-005
must eventually resolve this; until then the snapshot is the only rollback.

## 2026-08-04 R11-D1A ratification

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R11-D1A-JUDGE-TEMPERATURE-SUFFICIENCY-PROVENANCE | **Accepted — all four as recommended** | Temperature 0 retained; repeats reclassified as determinism checks; prose correction authorized; retrospective manifest authorized as *recovered, not proven*; coherence v1.0 reported and non-gating with `concept_relevance` remaining the sole preregistered acceptance metric. Verbatim rationale in `VERIFICATION_LOG.md`. | R11-C00 is unblocked. R11-D2 may be scoped. No credentials, paid calls, or production judging are authorized. |
| R11-C00-TEMP0-RELIABILITY-PROSE-CORRECTION | **Active — Engineer 1** | Thirteen pre-located sites across four files, independently verified accurate by the Orchestrator. Deck `sae.pptx` verified clean; blocking scope is spoken delivery only. | Prose/evidence only. Independent audit required before publication. |
| R11-D1A-PRESENTATION-RISK | **Standing rule** | Slide 5 teaches α as the steering-scale symbol. | All French oral/revision material spells "Krippendorff alpha" in words; never the bare symbol. |

## 2026-08-04 R9 tip reconciliation (Orchestrator repository evidence)

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-C4-ED36-BUILDER-FINAL-CORRECTION | **Engineer complete — commit `1ed3ad98bad03e084a7237263ea7c9d0b69b3a4d`** | Dated 2026-08-04 00:15 on branch `r9-ed36-bundle-builder`, parent `490ae73`. Changes 4 files (+1467/-109): builder, acquisition schema v1, and two test modules. Was absent from all `project_management/` files until this entry. | Candidate exists; acceptance is not implied by its existence. |
| R9-V3-ED36-BUILDER-REACCEPTANCE | **Verdict not in durable memory — reconstruct** | Detached audit worktree `D:\qwen-sae-interp-r9-v3-audit-1ed3ad98` exists at exact `1ed3ad9`, so an audit ran against C4. No verdict, evidence, or finding list was written to `VERIFICATION_LOG.md`. The only durable trace is the successor commit message asserting F1–F4 findings and that builder construction logic was "certified adequate by V3". | Documentation defect. The F1–F4 finding text must be recovered from the Auditor 2 conversation, not inferred from an Engineer commit message. |
| R9-V4 (label) | **Naming defect — a `V` identifier was used for a code commit** | `82b028e160e62abe6012b60965944b77413817dd`, dated 2026-08-04 01:45, is an implementation commit titled `R9-V4: close four builder-repair gaps (F1-F4)`. Verification identifiers must not name commits. | Rename the repair lane `R9-C4B`; reserve `R9-V*` for audits. |
| R9-C4B-ED36-ACQUISITION-MANIFEST-GAP-CLOSURE | **Engineer complete — commit `82b028e`, unaudited** | Diff from `1ed3ad9` is 3 files (+445/-11): 9 negative build-system tests (F1), isolation `returncode` fail-closed at schema and code (F2), conditional `sha256`/`size_bytes` for `type: "file"` (F3), dedicated `derived_backend_record` with constrained-empty `backend_path` (F4). Cumulative diff `4bf0fd8..82b028e` is **exactly the seven authorized paths**; no eighth path. | Scope authority is satisfied. Behavioral acceptance is not. |
| R9-V5-ED36-BUILDER-FINAL-ACCEPTANCE | **Queued — Auditor 2** | No audit worktree exists at `82b028e`. R9-V3 audited `1ed3ad9`; the tip changed production code (`_validate_derived_wheels`, `_validate_derived_entry_shape`) and the schema after that audit. | The R9 branch tip has **zero independent verification**. All external ED-36 work remains blocked. |

### Orchestrator source findings carried into R9-V5

- `_require_exact_keys` accepts `optional: set[str] | None = None`, so F4's removal of the `optional=` argument is runtime-valid; the conditional `required_keys | {"backend_path"} if field == "backend" else required_keys` binds as intended (`|` outranks the conditional expression).
- Native-side asymmetry is only **partly** closed. At `environment_bundle.py:3693-3697` validation checks `native_connection_attempt.returncode` but never `native_connection_attempt.succeeded`, while the python side checks `succeeded is not False`. A manifest asserting `succeeded: true` with a non-zero `returncode` is internally contradictory and currently admitted.
- Build-time isolation remains fail-closed at `environment_bundle.py:1212`.
- Branch `r9-ed36-bundle-builder` forks from `4bf0fd8`; `origin/main` has since advanced to `9d90ef6` (`2e8efb0` stats prose, `9d90ef6` census launcher). The two overlays are path-disjoint, so a later integration item is required but no conflict is expected.

## 2026-08-03 R9-D4 decision

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-D4-DERIVED-RECORD-SCHEMA-SCOPE-RATIFICATION | **Accepted** | Exactly path 7, `schemas/environment_acquisition_manifest/v1.schema.json`, was added to cumulative R9-C4 authority; install schema and frozen export require no present edit. | Engineer 2 may resume R9-C4; no external construction or execution follows. |
| R9-C5-REAL-DERIVED-WHEEL-RUNTIME-EXPORT | **Pre-recorded deferred gate** | The future real wheel filename/hash must replace the sdist in the bundle/runtime requirements truth only after that wheel exists. | Prevent an accidental eighth path during R9-C4. |

## 2026-08-03 R9-A3 architecture closure

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-A3-DERIVED-BUILD-REQUIREMENT-SEMANTICS | **Complete** | Option 2 adopted; the sdist hash remains export-authorized, the produced wheel is authorized only by its complete derived record, compatible PEP 508 ranges may map to the sole exact locked artifact, and kernel network isolation is mandatory. | Builder semantics are settled. |
| R9-D4-DERIVED-RECORD-SCHEMA-SCOPE-RATIFICATION | **Queued — Human/Program Manager** | Exact-commit schema inspection proves the strict v1 `derived_wheel` object lacks required provenance fields. | Approve the acquisition-manifest schema as the sole seventh path before R9-C4. |

Audit: `AUDIT-01`  
Revision: `3ac9e23` plus inherited working-tree changes  
Acceptance rule: files and passing synthetic tests prove local
implementation, not production completion.

## Bounded work-item ledger

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| WF-LAB-ASSISTANT-ROLE | **Accepted — workflow role established** | Durable bootstrap defines TamIA execution/evidence authority, preflight requirements, evidence labels, prohibitions, and routing boundaries. | Fully specified cluster work routes to `LAB_ASSISTANT`; scientific decisions remain Researcher-owned and acceptance remains Auditor-owned. |
| R0-CI-LINT | **Accepted** | Diff confined to the two authorized ad hoc scripts; imports/noqa cleanup and one local variable rename only. Current exact CI Ruff command passes. Verifier clean-environment run: 603 passed, 3 deselected. Lock and diff checks pass. | CI lint baseline restored with no artifact, interface, numerical, or scientific-output change. |
| R1-CONFIG-LIFECYCLE | **Accepted** | Shared dependency-legal lifecycle path applied to all nine jobs. Engineer full suite: 606 passed, 3 deselected. Independent matrix: nine job failure paths plus six import-contract tests passed. Ruff, lock, and diff checks green. | Readable malformed/non-mapping/schema-invalid configs now return 3, write exactly one failed A10, and enter no heavy work/domain writes; valid lineage and missing-path behavior remain unchanged. |
| R2-DOC-CONTRACT-DRIFT | **Accepted** | Diff confined to the five authorized files and to prose/comments. Independent focused tests: 8 passed. Exact CI Ruff, lock, and diff checks green; Engineer full suite: 606 passed, 3 deselected. Repository searches confirmed the corrected config, package, job, dependency-pause, and 128-ULP facts. | Five factual contradictions closed without executable, schema, artifact, golden, threshold, dependency, or ED change. |
| R3-WP8-JUDGE-PRODUCER | **Accepted (local scope)** | R3-C1/R3-C2 corrected grid validation, scalar handling, exit classification, lineage sealing, and empty-grid behavior. Engineer full suite: 628 passed, 3 deselected. Orchestrator matrix: 135 passed. R3-V2 Verifier: 19 passed plus hostile mutation and empty-grid probes accepted. | Local A9→A9′ producer contract is complete; live Lodestar and production evidence remain explicitly unverified under ED-19. |
| R3-C1-JUDGE-BOUNDARY-VALIDATION | **Engineer complete** | Complete source grid now succeeds; missing/duplicate/mismatched cells fail as input-contract exit 3; wrong artifact type exits 3; fractional/boolean token counts and non-finite runtime scores/perplexities exit 4. No failure writes A9′. | Correction evidence is sufficient to route to independent verification. |
| R3-V1-JUDGE-PRODUCER-VERIFICATION | **Needs correction** | Verifier: focused 17/17 and preserved-contract 133/133 green. Adversarial runtime changed `sampling.seed`; job exited 0 and A9′ incorporated the mutation. A zero-record/zero-prompt bundle also exited 0 with zero scores. | ED-21 identical-payload lineage and non-vacuous evidence coverage remain violated. |
| R3-C2-JUDGE-IMMUTABILITY-EMPTY-GRID | **Accepted** | Sealed source/subject/payload/slice snapshots build A9′; runtime receives disposable deep copies. Empty records are rejected before runtime. Engineer focused: 19 passed; full: 628 passed, 3 deselected. Independent matrix: 135 passed. | R3-V2 confirmed both corrections. |
| R3-V2-JUDGE-CORRECTION-REVERIFICATION | **Accepted** | Verifier: 19 passed; hostile mutation preserved source/config/slice bytes and sealed A9′ lineage; empty grid exited 3 before runtime with one failed A10 and no A9′. | R3 accepted for local implementation scope. |
| R4-A002-CHECKPOINT-SCOPE-PACKET | **Accepted — resolved by ED-35** | ED-35 now records five distinct, eligible, existing final checkpoints: `d1bgp5v5`, `rwu04lpb`, `zf2o13m2`, `o1cx1dow`, and recovered L28×16 `hm03l7yz`. `a520ytu6` is an intermediate; Montreal/poutine is retired; W&B IDs are telemetry. | The literal “6+” estimate is superseded without changing the frozen architecture. The five-checkpoint set governs certification coverage and band-calibration provenance. |
| R4-V1-L28X16-COMPLETION-AUDIT | **Inconclusive — environment limitation** | Full-history inspection found candidate `hm03l7yz` and expected path `results/sae_checkpoints/hm03l7yz/final_400001024` in historical survey/steering launchers. No committed completion record exists. W&B lacked authentication and Tamia rejected SSH, so neither authoritative surface could be searched. | Candidate evidence cannot be promoted or dismissed. WP2 remains open; the next action is external access/evidence, not implementation or repeat local audit. |
| R4-X1-L28X16-AUTHORITATIVE-ACCESS | **Accepted — checkpoint recovered** | Authoritative Tamia access confirmed `hm03l7yz/final_400001024` after eight intermediates. The full SAELens checkpoint is L28×16 (`d_sae=81920`), TopK k=100, and 6.44.2 baseline-compatible. W&B is unrecoverable but non-blocking under ED-30. | WP2 population is settled at five. `hm03l7yz` requires the ordinary A5 backfill and A6 certification pipeline; it is not an unknown/recovery gap. |
| R5-X1-HM03L7YZ-A5-BACKFILL | **Accepted** | Commit `04e88dc` adds only the backfill config, verbatim runner config, A5 `3e6fdcb1187a`, and completed A10 `ada8ac14bd48`. Independent envelope/schema/self-hash validation passed. A5 weights subject equals the reported on-cluster ED-27 hash `0f8670c078e8…`; source was preserved. | The authoritative population now has five A5 manifests. `hm03l7yz` is ready for its separate production A6 certification. |
| R5-X2-HM03L7YZ-A6-CERTIFICATION | **Blocked before resubmission — ED-36 evidence chain incomplete** | Launcher defects are closed and ED-36 settles environment policy. The local ED-36 candidate is unverified/uncommitted; no external acquisition/install manifests or exact 3.2.1/3.4.0 equivalence report exist. No fifth A6/A10 exists. | Resume only after R6-V1, a stable source commit, the external bundle/install evidence, and the ED-36 equivalence gate. |
| R5-C1-CERTIFY-LAUNCHER-WHOLE-NODE | **Accepted** | Commit `65ff603` changes only `launch_certify.sh`: `48G→mem=0`, `h100:1→h100:4`, plus the adjacent calibration comment. Bash syntax, stubbed submission arguments, preserved fields, and diff checks passed. Independent diff inspection confirms scope and unchanged runtime command. | Canonical production certification submission is unblocked; no separate Verifier stage is required for this two-value resource calibration. |
| R5-C2-CERTIFY-LAUNCHER-BASH-WRAP | **Accepted** | Commit `70b7ed8` changes only the cert launcher. R5-V1 proved the exact captured `%q` payload survives Tamia `/bin/sh`, enters login Bash, initializes modules, resolves the venv remotely, and reaches the exact harmless stub arguments. | The launcher shell boundary is closed; no further launcher correction is indicated. |
| R5-V1-CERTIFY-WRAP-TAMIA-PREFLIGHT | **Accepted** | Tamia host-only proof exited 0 with exact launcher/config blobs, module initialization, fake activation, exact certify arguments, cleanup, and zero scheduler/artifact mutation. | Production submission is no longer blocked by launcher parsing. |
| R6-A004-CLUSTER-ENV-REBUILD-POLICY | **Accepted — closed by ED-36** | ED-36 authorizes an external, retained, hash-verified offline bundle derived from the committed lock; forbids cluster network installation; selects production TransformerLens 3.2.1; and requires exact 3.2.1/3.4.0 equivalence evidence before R5-X2. | Architecture is settled. Local implementation and external evidence remain separate acceptance gates. |
| ED-36-LOCAL-REPOSITORY-PORTION | **Needs correction** | Dependency truth/export and schema discovery are correct, but R6-V1 found unusable bootstrap, incomplete marker support, porous manifest/derived-wheel/path/installed-closure checks, false dirty-source revision, and cert-lane lifecycle/equivalence defects. | Repair in dependency order; no external construction or production use. |
| R6-V1-ED36-LOCAL-VERIFICATION | **Needs correction** | Verifier reproduced the export byte-for-byte and passed 649 tests, but adversarial probes demonstrated the bootstrap and semantic bypasses. Exact CI Ruff also has seven findings. | R6-C1 repairs the validator foundation; a later item repairs cert-lane lifecycle before re-verification. |
| R6-C1-ED36-BUNDLE-VALIDATOR | **Engineer complete — combined re-verification pending** | Candidate now uses a stdlib-only outside-repo bootstrap; validates virtualenv identity, real-export markers, exact runtime/tooling/installed closure, resolved path containment, derived-wheel provenance, and clean source revision. Engineer: 25 focused, 52 cert-lane, 659 full-suite tests passed; Ruff/lock/Bash/diff green. | Proceed to the separately bounded lifecycle correction, then independently re-verify both corrections together. |
| R6-C2-ED36-CERT-LANE-LIFECYCLE | **Engineer complete — combined re-verification pending** | Candidate validates config first; maps valid-config evidence faults to one exit-4 A10; semantically links acquisition/install evidence; and requires checkpoint/stream/version/hook/equality/sanity-valid R5-X2 equivalence evidence. Engineer: 48 focused and 676 full-suite tests passed; Ruff/lock/diff green. | Re-verify with C1 as one trust boundary before stabilization or external work. |
| R6-V2-ED36-COMBINED-REVERIFICATION | **Needs correction** | Export/markers, bootstrap mutation safety, most bundle attacks, installed closure, four-job lifecycle, and standard equivalence cases passed. Remaining failures: spoofable virtualenv identity, conflicting runtime/tooling overlap, fabricated module fallback, weaker C2 schema/target/derived validation, fabricated revision, and non-authoritative R5-X2 config. | R6-C3 closes the final measured semantic gaps before re-verification. |
| R6-C3-ED36-SEMANTIC-CLOSURE | **Needs correction after R6-V3** | Export/overlap/config anchoring passed, but creator internal identity/postconditions/rollback, admission replay, CUDA evidence, versioned module matching, one schema parity case, and failed-A10 addressability remain defective. | Superseded by bounded R6-C4 correction. |
| R6-V3-ED36-FINAL-LOCAL-REVERIFICATION | **Needs correction** | Focused ED-36: 32 passed/6 failed; lifecycle/schema/import: 133 passed/1 failed; full: 670 passed/7 failed/3 deselected. Lock/Ruff/Bash/diff green. | R6-C4 addresses only the seven failing behaviors; no external work. |
| R6-C4-ED36-CREATOR-ADMISSION-HARDENING | **Engineer complete — final verification pending** | Candidate validates creator wheel metadata, functional venv postconditions and rollback; replays admission; accepts versioned modules; restores schema parity and canonical failed-A10 config refs. Full pytest/Bash evidence was environment-blocked. | Preserve these corrections through R6-C5, then independently verify the combined candidate. |
| R6-C5-ED36-ALLIANCE-CUDA-IDENTITY | **Engineer complete — final verification pending** | Candidate accepts exact approved `2.13.0+computecanada`, preserves locked public 2.13.0, records CUDA from live torch, and admits only exact artifact/install/live-runtime agreement. Focused tests/probes and lock/Ruff/diff passed; full pytest/Bash were environment-blocked. | R6-V4 independently verifies C1–C5 before stabilization. |
| R6-V4-ED36-FINAL-ACCEPTANCE | **Needs correction** | Most trust-boundary checks passed, but creator TOCTOU allowed substituted bytes to execute under the approved hash, and torch admission accepted unauthorized `+cu121` with arbitrary hash/no retained file. Full suite: 693 passed, 7 failed, 3 deselected. | R6-C6 closes the two roots of trust and stale-fixture failures before re-verification. |
| R6-C6-ED36-IMMUTABLE-CREATOR-TORCH-ANCHOR | **Engineer complete — verification pending** | Candidate seals creator bytes into an execution snapshot, verifies the child-boundary hash, reports the executed hash, requires retained exact `torch==2.13.0+computecanada` artifact replay, and repairs legacy fixtures. Direct negative-case probes passed, but pytest and Bash evidence were host-permission blocked. | R6-V5 must independently verify both immutable-execution and exact-torch roots of trust and run the combined C1–C6 regression suite. |
| R6-V5A-ED36-REMAINDER-ACCEPTANCE | **Needs correction** | Auditor: focused ED-36 61 passed/7 failed; full suite 702 passed/7 failed/3 deselected; exact torch 20 passed; lifecycle matrix 24 passed. Two tests expect a later diagnostic even though validation already fails closed, while five install-record fixtures omit the now-required creator wheel and never reach their stated assertions. Exact-byte execution remains unverified. | Repair the seven tests and add the three missing torch-specific boundary cases; do not weaken earlier validation. Overall R6 remains open. |
| R6-C6A-ED36-TEST-FIXTURE-COVERAGE | **Absorbed into R6-C7** | The seven V5A test repairs and three missing torch cases touch the same authorized test module required by C7 and are necessary for C7's full-suite-green acceptance criterion. | Keep them explicit inside one two-file final-local-closure work order; do not create an overlapping second edit lane. |
| R6-A006-EXACT-BYTE-EXECUTION-TEST-CONTRACT | **Accepted — no ED amendment** | Architect confirmed a real two-read production gap: the child hashes the snapshot, then zipimport rereads the mutable path. Decision requires single-buffer verification/execution, parent post-child rehash before venv validation, and one named real-subprocess regression. | The implementation ambiguity is closed; execute the bounded two-file correction. |
| R6-C7-ED36-FINAL-LOCAL-CLOSURE | **Needs correction after Orchestrator review** | Engineer reports 714 passed/3 deselected, green Ruff/lock/Bash, and passing post-fix named behavior. However production child code exports `INTERPLAB_CREATOR_SNAPSHOT_PATH` solely for the test, and the named test wraps `bundle.subprocess.run`; both violate A-006's no-production-test-hook and real-unmocked-subprocess contract. The pre-fix failure was also a `SyntaxError`, not the intended invariant failure. | Preserve the single-buffer and parent-rehash correction, but remove the environment seam and make the test invoke the child without replacing `bundle.subprocess.run` before V5B. |
| R6-C8-ED36-REMOVE-CREATOR-TEST-SEAM | **Engineer complete — V5B pending** | Production environment seam and test subprocess wrapper are removed; named test uses the real child path and reports A-only execution, exact mismatch, rollback, prefix cleanup, and sentinel preservation. Engineer: environment bundle 73 passed; full suite 714 passed/3 deselected; Ruff/lock/Bash/diff green. | Independently run the existing named test unchanged and audit source/no-seam invariants. |
| R6-V5B-ED36-FINAL-LOCAL-VERIFICATION | **Accepted for exact local candidate** | Hash-bound audit of `environment_bundle.py` (`740dd611…`, 93,984 bytes) and `test_environment_bundle.py` (`7bbc1152…`, 61,153 bytes): named test 1, environment 73, creator 12, torch 23, repaired V5A 7, lifecycle 24, full suite 714 passed/3 deselected; Ruff/lock/Bash/diff green. Start/end hashes and 34-modified/16-untracked status matched. | Candidate behavior is accepted. R6 is not reproducibly complete until selective stabilization and exact commit verification. |
| R6-V5B-AUDIT-RECONCILIATION | **Closed — Auditor 1 evidence superseded** | Auditor 1 reported the old 7-failure V5A signature, old A006→C7 routing, and no candidate hashes. Current files exactly match Auditor 2’s start/end hashes and corrected 73-test/714-test evidence. | Do not reopen corrected fixtures from the stale audit; preserve it as historical evidence about an earlier candidate. |
| R6-S1-ED36-CANDIDATE-STABILIZATION | **Engineer complete — commit `c6ef2df`** | One local commit with parent `70b7ed8` contains exactly the 46 authorized paths. Named/environment/full 714-test suites and static checks passed; V5B core hashes survived; tracked worktree is clean. Four collapsed untracked exclusions remain untouched. | Independently verify the commit tree and execute committed bytes from a clean detached worktree. |
| R6-V5C-STABILIZED-COMMIT-VERIFICATION | **Accepted reproducible local revision** | Clean detached checkout of `c6ef2df`: exact parent/message/46-path manifest, 12/12 new files, 3/3 schemas, accepted hashes, named 1, environment 73, full 714/3-deselected suite, Ruff/lock/Bash/diff all green. Detached and main states cleaned/preserved. | R6 local gate is closed. Push, external ED-36 evidence, equivalence, R7/T1.2 integration, fifth certification, and cluster execution remain separate. |
| R8-I1-R7-LAUNCHER-INTEGRATION | **Active — Engineer 2** | Integrate accepted R7 commit `b7aad6a` onto reproducible base `c6ef2df` in a new isolated integration worktree. Preserve the accepted R2 steer-header correction while applying the four launcher wrap/resource changes. | Produce a local integration commit only; no main mutation, push, T1.2 integration, or cluster submission. |
| R7-C1-LAUNCHER-FIX-PROPAGATION | **Accepted — isolated branch; integration deferred** | Commit `b7aad6a2e25a45c5b4fab48951b5bfd92a47ae53` on local branch `r7-launcher-propagation` changes only the four authorized launchers. Bash syntax and non-submitting scheduler probes passed 4/4; decoded payloads, deferred venv expansion, preserved arguments, and exact resource requests were verified. Active R6 tree and `main` remain untouched. | Retain the isolated worktree/branch. Merge only after R6 stabilization, preserving the accepted R2 steer-header correction, then run integration checks. |
| T1.2-ABLATION-9056-PREP | **Scientifically specified — execution gated** | Q-011-C4 fixes battery v1.1.0, author `Mohamed El Yazid — IID`, exact change record, probes/markers, seed scheduling, A2→A8 chain, statistics, QC, `n_docs: 20000`, and environment gates. | The packet needs one factual correction and independent verification; production execution still requires integration, R6/R7, A3/A7/A8, and Lab Assistant preflight. |
| T1.2-C1-PROTOCOL-PACKET-CORRECTION | **Needs correction — isolated commit `c4f0da7`** | Fourteen-file packet was committed locally; schemas, golden boundary, battery/census/validate/steer tests, lock, and diff passed. Full pytest and one characterize test were unverified. The characterize config still falsely says characterize emits A8, labels approved `n_docs: 20000` as awaiting confirmation, and several gates predate the accepted A-006 decision. | Apply a documentation/config-comment-only C2 correction on the same branch, then route the entire packet to independent verification. |
| T1.2-C2-PACKET-FACTUAL-GATE-CORRECTION | **Needs correction — moving-gate prose only** | Commit `e92174a` correctly fixes A7/A8 ownership and approved `n_docs: 20000`; four YAMLs are semantically unchanged, characterize is 13/13, and full pytest is 603 passed/3 deselected. Concurrent R6 review returned C7 and inserted C8, making the newly written `C7→V5B` gate immediately stale. | Replace volatile R6 sub-item IDs with the stable condition “R6/ED-36 local acceptance”; preserve all other C2 changes. |
| T1.2-C3-STABLE-R6-GATE-WORDING | **Engineer complete — commit `e9ad361`** | Five authorized files now use the stable R6/ED-36 acceptance condition; four YAML objects remain semantically identical to `e92174a`; schemas, characterize 13/13, schema tests 24/24, lock, and diff checks passed. | No known packet defect remains; independently audit the complete C1–C3 packet. |
| T1.2-V1-PROTOCOL-PACKET-VERIFICATION | **Accepted for isolated preparation packet** | Clean `e9ad361`; exact 14-file scope; ED-8/battery/golden/cheese authority, 39 markers, chain/config/statistics/gates verified. Focused 148 and full 603/3-deselected passed; lock/diff green; only identical inherited Ruff findings. | Packet may be integrated. Production execution still requires combined integration, environment/equivalence, preflight, real A3/A7/A8, and filled hashes. |
| R8-I2-T12-PACKET-INTEGRATION | **Active — Engineer 1** | Apply audited T1.2 commits `c4f0da7`, `e92174a`, `e9ad361` onto accepted R6 base `c6ef2df` in a separate clean integration worktree. | Preserve exact 14-file packet semantics; no R7 integration, main mutation, push, registry write, or cluster action. |
| P0-PI-PRESENTATION-EVIDENCE-PACKET | **Cancelled by Researcher** | Researcher explicitly requested actual ablation work rather than a presentation packet. | No Lab Assistant presentation task should run. Preserve existing evidence but prioritize the experiment critical path. |

## WP0-WP10 reconciliation

| WP | Implementation state | Verification / production state | Classification | Acceptance |
|---|---|---|---|---|
| WP0 Contracts bootstrap | Core plumbing, registry, all A1-A12 schemas, ten job-config schemas, fixtures, and contract tests exist. | Default suite, schema tests, and all 15 live registry artifacts are green. | Locally implemented and verified. | **Accepted for local contract scope.** |
| WP1 SS7 library | `spec`, `hooks`, and `control` exist with identity, golden, hygiene, measurement, and validation tests. | Tiny-fixture evidence passes. Two real-Qwen nightly identity tests skip because `INTERPLAB_NIGHTLY_QWEN_DIR` is unavailable. | Verification gap + environment limitation. | **Conditional.** |
| WP2 SS4 certify | Backfill/certify code, schemas, wrapper, launcher, metrics, bands, and tests exist. Five A5 and four A6 production artifacts validate. ED-35 establishes `hm03l7yz` as the fifth eligible final checkpoint on the same 6.x baseline. | Launcher fixes are accepted and ED-36 settles environment policy. The local ED-36 implementation still requires R6-V1 and a stable commit; the external bundle/install manifests and exact 3.2.1/3.4.0 equivalence report remain absent. A10 recovery for the older four remains a separate WP7 item. | Local verification/stabilization, external environment evidence, equivalence, then production execution. | **Pending R6-V1 and fifth A6; not yet accepted.** |
| WP3 SS1 corpus | Battery, manifest, replay, census, schemas, job, wrapper, and tests exist. A1/A3 plus their RunCard validate. | A1 dataset revision is `unknown`; most non-English census rows lack researcher-authored terms; no complete-language sensitivity content exists. | Implementation present; researcher/provenance limitations. | **Conditional.** |
| WP4 SS5 + SS2 | Feature index, indexer, dashboards, model loading, store QA, jobs, schemas, wrappers, and synthetic integration tests exist. | A4 and A7 registries are empty. `characterize_lite.py` explicitly bypasses the job, RunCard, A7, and dashboards. Legacy streaming checkpoints legitimately have no A4 store. | Production verification gap; A4 campaign limitation intentional. | **Conditional / not production-accepted.** |
| WP5 SS6 validate | Specificity, sensitivity, selectivity, probe, bands, judge abstraction, job, schema, wrapper, and tests exist. | No A8 exists. Only deterministic stub/no-op rubric judges are available by design. Real battery v1 has no complete language, and bands are placeholder. | Verification gap + researcher action + intentional non-capability. | **Conditional / not production-accepted.** |
| WP5b stats | All four frozen functions and independent reference tests exist and pass. | Reference and ordinary tests pass. | Locally implemented and verified. | **Accepted.** |
| WP6 SS9 reports | Chain assembly, statistics composition, renderers, job, schema, wrapper, and synthetic end-to-end tests exist. | Required cheese dose-response pilot is absent; A9/A11/A12 are empty. Local internship reports are not A11 artifacts. | Required package output absent + downstream evidence gap. | **Partial / not accepted.** |
| WP7 SS10/SS12 | Registry/RunCard code, ten current jobs, wrappers, sync job, and five launchers exist. R1 fixes readable-invalid-config exit/RunCard behavior across the implemented jobs; R2 corrects the steer launcher description. | Four production certify runs have no local A10. No production sync/failure-interruption evidence exists. | Lineage and production-verification gaps. | **Partial / not accepted.** |
| WP8 SS8 boundary + steer | Blinding, capability payload assembly, compat-map I/O, steer and judge job surfaces, schemas/wrappers, and tests exist; R3 is accepted locally. | A9/A12 production registries are empty and ED-19 keeps live Lodestar paused. Architect review found no genuine ambiguity and issued no ED amendment. | Local implementation accepted + intentional pause + production verification gap. | **Conditional / not production-accepted.** |
| WP9 canary + snapshots | Canary comparator/reference schema/nightly test and battery/tokenization snapshots already exist; mechanism tests pass. | Real `cheese_reference.json` is absent, so the real canary skips explicitly. WP5/WP6 are not accepted prerequisites. | Existing implementation + researcher evidence gap. | **Not released; do not start new work as “the only remaining WP.”** |
| WP10 repository hygiene | CI workflow exists; current inherited script edits make Ruff green. A-003 establishes canonical `project_management/` as intentionally local-only. | The commit label “WP0-WP10” is not evidence. Scientific `reports/`/curated-result tracking remains unresolved under A-005; docs and the final governance pass remain open. | Documentation/release-governance gaps. | **Not accepted.** |

## Blueprint and ED obligation map

| Obligations | Implementation and tests | Audit result |
|---|---|---|
| ED-1, ED-19, ED-32, ED-33: single dependency truth and SAE-stack baseline | `pyproject.toml`, `uv.lock`, `core.environment`, cert-lane job checks, environment tests | Clean install, lock, and local tests pass. Lodestar remains intentionally paused. Real-Qwen nightly evidence absent. |
| ED-2: canonical JSON | `core.canonical_json`, envelope/hash tests | Implemented and green. |
| ED-3, ED-4, ED-22: intervention numerics, positions, controls, prompting baseline | `interventions/*`, `jobs.steer`, A9/steer schemas, intervention and steer tests | Local implementation green; no production A9; real identity tests skipped. |
| ED-5, ED-6, ED-7, ED-27, ED-29-ED-31, ED-34: certification, provenance, RunCards, GPU/URI path | certification/jobs/core modules, A5/A6/A10 schemas, backfill/certify tests, launcher | R1 closes the readable-invalid-config lifecycle defect. Four production A5/A6 validate; missing cert-run A10s, placeholder bands, and the “6+” scope gap still prevent acceptance. |
| ED-8-ED-10, ED-13, ED-28: battery authorship, census semantics, validation availability | corpus/validation modules, battery/A1/A3/A8 schemas, corpus/validation tests | Mechanisms green. Researcher content incomplete; A8 absent. |
| ED-11: store QA and bands | `store_qa/*`, A4 schema, job/tests | Locally green; no production A4; bands placeholder. |
| ED-12: characterization index layout/API | `characterization/*`, A7 schema, tests | Locally green; no production A7. |
| ED-14-ED-17: chain, reports, stats, immutable evidence | reports/stats modules, A11/A9 schemas, chain/render/stat tests | Synthetic evidence green; required pilot and production A11 absent. Reports are not tracked as required. |
| ED-18: package assignment | steer exists under WP8; training remains absent/dormant | Steer implemented. Training pause is explicit, not a defect. |
| ED-20-ED-21: judged A9′ and capability | A9 schema, evaluation adapter/capability boundary, judge job/config/wrapper, and adversarial tests | R3 accepted locally after independent mutation, grid, scalar, lineage, and failure-path verification. Live Lodestar and production A9/A9′ remain absent under ED-19. |
| ED-23: canary/reference behavior | `tests/canary_lib.py`, canary schema/test, snapshot tests | Mechanism green; real reference/recompute unavailable and explicitly skipped. |
| ED-24: hygiene/equivalence | CI workflow, Ruff, git/ignore state | Current dirty tree is green. R2 closed five factual drifts; tracking/governance obligations remain. |
| ED-25/ED-26 ledger continuity | ED-25 is absent; ED-26 appears in tests and later prose | Documentation defect remains. R2 reconciled the §8.2 golden-test row to ED-33's existing 128-ULP contract. |

## WP9 gate

WP9 is **not safe to begin as the next package**. Its mechanism is already
present, while its scientific reference is researcher-owned. The correct
next actions are to close or explicitly waive WP2/WP4-WP8 acceptance gaps,
resolve the researcher-owned checkpoint population, and obtain the
outstanding production evidence before authorizing further package work.

## Integration acceptance update

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R8-I2-C1-INTEGRATION-BYTE-FIDELITY | **Accepted** | Detached LF-faithful checkout of `7597af0`; authoritative requirements and hm03 config hashes matched before/after. Environment bundle 73, full 714 passed/3 deselected, Ruff, lock, diff, and clean-status checks passed. | R8-I2 T1.2 integration is accepted without any content change. |
| R8-I2-T12-PACKET-INTEGRATION | **Accepted** | Exact 14-path/14-blob audited overlay plus byte-faithful full-suite acceptance at `7597af0`. | May enter combined R7+T1.2 integration; cluster execution remains unauthorized. |
| R8-I3-R7-T12-COMBINED-INTEGRATION | **Queued — Engineer 2** | R7 candidate `a65dfb4` is a single four-launcher commit on `c6ef2df`; T1.2 accepted integration is `7597af0`. Overlays are path-disjoint. | Assemble and verify an exact 18-path LF-faithful candidate before external ED-36/preflight work. |

## Combined integration update

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R8-I3-R7-T12-COMBINED-INTEGRATION | **Engineer complete — verification pending** | Candidate `4bf0fd8` has exact four-commit ancestry, exact 18-path union, 14/14 accepted packet blobs, preserved R2 steer header, correct launcher behavior, authoritative hashes, environment 73, full 714/3-deselected, and green static checks. | Do not push or execute; independently audit the exact commit from a fresh LF-faithful checkout. |
| R8-V1-COMBINED-INTEGRATION-ACCEPTANCE | **Queued — Auditor 2** | Major milestone audit of combined R6+R7+T1.2 candidate `4bf0fd8`. | Acceptance gates repository integration only; ED-36 external evidence and production experiment artifacts remain separate. |

## Combined audit correction

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R8-V1-COMBINED-INTEGRATION-ACCEPTANCE | **Needs correction — non-independent report** | The purported audit reused the Engineer worktree, created no detached audit checkout, and repeated Engineer results. Repository inspection confirms the requested audit path was absent. | Candidate remains clean but cannot be accepted as independently verified. |
| R8-V1-C1-INDEPENDENT-CHECKOUT-RETRY | **Queued — Auditor 2** | Exact commit and audit contract are unchanged; only execution from a new detached LF-faithful worktree is missing. | Complete the independent gate without any implementation change. |

## Combined acceptance update

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R8-V1-C1-INDEPENDENT-CHECKOUT-RETRY | **Accepted** | Fresh detached audit at `4bf0fd8`; exact path/import/ancestry/scope/blob/schema/placeholder/launcher/hash evidence; environment 73, focused 103, full 714/3-deselected, and all static gates green. | Combined R6+R7+T1.2 local revision is independently accepted. |
| R8-V1-COMBINED-INTEGRATION-ACCEPTANCE | **Accepted** | Independent retry closes the first report's procedural defect without any content change. | Exact `4bf0fd8` may be considered for publication; it is not yet on the shared remote. |
| R8-X1-PUBLISH-COMBINED-REVISION-AUTHORIZATION | **Awaiting Researcher authorization** | Pushing exact accepted commit to `origin/main` is required for reproducible Tamia consumption and is an external state change. | Pause until explicit commit-and-branch authorization is supplied. |

## Publication and cluster-routing update

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R8-X1-PUBLISH-COMBINED-REVISION | **Accepted** | Authorized fast-forward moved `origin/main` from `70b7ed8` to exact audited `4bf0fd8`; local remote-tracking ref confirms equality. No force, merge, rebase, new commit, file change, or cluster action. | Tamia can now reproducibly fetch the accepted revision. |
| R9-X1-TAMIA-PREFLIGHT-AUTHORIZATION | **Awaiting Researcher authorization** | Publication explicitly excluded cluster execution. Next proposed action is safe Tamia fast-forward plus read-only prerequisite discovery, with no scheduler/environment/scientific mutation. | Pause before any Tamia access until the bounded authority is explicit. |

## Tamia preflight and ED-36 construction update

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-X1-TAMIA-PREFLIGHT | **Needs correction — procedural deviation; evidence retained** | Tamia reached exact `4bf0fd8` and read-only inventory completed with zero jobs/artifacts, but the fast-forward occurred despite one dirty tracked file and an explicit dirty-tree stop condition. All dirty/untracked state was preserved. | Existing checkout cannot serve as clean ED-36 source evidence; use a separate clean worktree. No rollback is indicated. |
| R9-A1-ED36-BUNDLE-CONSTRUCTION-PROTOCOL | **Queued — Architect** | ED-36 validators and installer exist, but no bundle/acquisition-manifest construction tool or exact external procedure is defined. | Architect must settle manual-procedure versus missing-builder ownership before Lab Assistant provisioning. |

## Parallel experiment preparation

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-P1-T12-EXECUTION-STAGING-PACKET | **Queued — Lab Assistant** | Scientific protocol and combined revision are accepted, but environment/equivalence and lineage artifacts are absent. | Prepare exact dry-run commands and evidence handoffs only; no Tamia access, config mutation, or result generation. |

## R9 architecture and preparation update

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-A1-ED36-BUNDLE-CONSTRUCTION-PROTOCOL | **Accepted — no ED amendment** | Both Architect reviews require checked-in mechanical construction; the conservative contract covers target/tags, tooling, two source-only derived wheels, Alliance torch, transfer/finalization, setup hardening, and mandatory pip check. | Route combined local implementation to Engineer 2; manual construction remains prohibited. |
| R9-C1-ED36-BUNDLE-BUILDER-AND-PIP-CHECK | **Queued — Engineer 2** | Fully specified five-path local implementation boundary; no network, bundle, Tamia, or venv action. | Independent audit required before any construction authorization. |
| R9-P1-T12-EXECUTION-STAGING-PACKET | **Accepted for preparation only** | Exact producer commands/handoffs, snapshots, logs, resources, substitutions, and stop rules mapped; zero execution/artifacts. One ref-only fetch disclosed. | Operational gaps are separated into parallel implementation and Researcher-decision lanes. |

## Q-013 and current parallel results

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| Q-013 | **Accepted — scheduler envelope corrected** | Census uses proven whole-node Tamia resources (`h100:4`, `mem=0`, baseline 8 CPUs), honest unknown-revision limitation, structural/blinded review, and four-check mechanical hash substitution. | R10-X1 determines walltime and whether fast-tokenizer parallelism justifies 16 CPUs. |
| R10-C1-T12-STATISTICS-DOC-CORRECTION | **Engineer complete — verification pending** | Clean one-file commit `2e8efb`; stale tests absent and authoritative bootstrap/equivalence rules present; lock/diff/scope green. | Route stable commit to Auditor 1. |
| R9-C1-ED36-BUNDLE-BUILDER-AND-PIP-CHECK | **Blocked correctly before mutation** | Clean base, zero edits; measured 110 active non-torch and two source-only packages. Exact tooling lock cannot be derived from local authoritative evidence. | Requires Human-authorized one-time networked tooling artifact bootstrap, then same Engineer resumes. |
| R9-X2-TOOLING-LOCK-ACQUISITION | **Authorized — Engineer 2 active** | One-time Linux CPython 3.11 tooling bootstrap with exact interpreter, universal pure-wheel validation, sole enumerated uv platform exception, immutable artifacts/transcript, and virtualenv seed-wheel inspection. | Resume R9-C1 only after exact evidence is retained and independently prechecked; no bundle, Tamia index, global install, or push. |
| R10-V1-T12-STATISTICS-DOC-VERIFICATION | **Accepted** | Independent audit of exact `2e8efb`: one-file scope, authoritative rules present, obsolete tests absent, other scientific sections byte-identical, lock/diff/clean state green. | Park commit for integration with the later accepted R9 successor. |

## R9 audit preparation update

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-V1-PREP-ED36-BUILDER-AUDIT-MATRIX | **Accepted for preparation** | Exact immutable identities and blocking matrix for scope, derivation, targets, tooling, derived wheels, torch, finalization, install/pip-check, TL separation, regressions, and retained state. | Run only after R9-C1 supplies a stable commit; does not prove external bundle/install/equivalence. |

## R9 tooling bootstrap correction

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-X2-TOOLING-LOCK-BOOTSTRAP | **Needs correction — safe stop** | CPython 3.11.15/14-artifact evidence retained; filelock/platformdirs overlap drift, missing pip origin, incomplete uv provenance, and unresolved virtualenv seed overlap remain. Zero repo edits. | Rebootstrap once with exact overlaps, direct origins, sole uv exception, and embedded seed inventory. |
| R9-D1-UNIVERSAL-PURE-WHEEL-TAG-RULING | **Accepted** | Universal ABI-none/platform-any wheels are eligible when Python-3/Requires-Python and byte/metadata checks pass; `uv==0.8.22` is the sole Linux-x86_64 platform exception. Virtualenv embedded seed wheels must be enumerated and seed setuptools must equal 83.0.0. | R9-X2B may perform one clean evidence rebootstrap under the existing network authority; any pin or embedded-seed conflict stops. |
| R9-V0-TOOLING-EVIDENCE-PRECHECK | **Active/queued — Auditor 2** | The replacement evidence root can be audited independently of the blocked code candidate. | Accept corrected evidence before R9-C1 resumes, preventing another implementation/bootstrap cycle. |

## Historical integration correction

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R8-I2-T12-PACKET-INTEGRATION | **Needs correction — checkout byte fidelity** | HEAD `7597af0` has the correct commit order, exact 14-path scope, and 14/14 accepted packet blobs. Focused 103, characterize 13, schema 24, Ruff, lock, and diff checks pass. Full suite is 7 failed/707 passed/3 deselected because global `core.autocrlf=true` expanded two accepted R6 working files while their Git blobs remained unchanged. | Do not edit or recommit content. Re-verify the exact commit from a command-scoped `core.autocrlf=false` worktree. |
| R8-I2-C1-INTEGRATION-BYTE-FIDELITY | **Queued — Engineer 1** | Accepted hashes are `9da00e03...314` for the frozen requirements export and `6dfb9e35...f326` for the hm03 certify config; current integration copies are CRLF-expanded. | Fresh byte-faithful checkout and tests only; no implementation, commit, push, or protected-worktree mutation. |

## 2026-08-03 parallel evidence recovery

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-V0A-TOOLING-EVIDENCE-PRECHECK | **Needs correction — evidence only** | Auditor recomputed 14 retained wheels and confirmed overlap conflicts, missing pip origin, absent uv artifact, and incorrect distlib classification without changing retained bytes. | R9-X2B replaces the evidence; R9-V0B must accept it before R9-C1 resumes. |
| R10-X1-CENSUS-RUNTIME-TOKENIZER-RECOVERY | **Accepted** | A10 found; job 382736 completed in 00:29:15; tokenizer revision exactly `cf98f3b3...`; fast backend but parallelism unenabled/unconfirmed; no mutation. | R10-C2 is unblocked at 1h, 8 CPUs, mem=0, H100:4. |
| R10-C2-CENSUS-LAUNCHER | **Queued — Engineer 1** | Exact scheduler and login-Bash contract is complete. | Independent non-submitting verification required before integration; no cluster execution authorized. |

## R10 census launcher implementation

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R10-C2-CENSUS-LAUNCHER | **Engineer complete — verification pending** | Commit `9d90ef6` adds one launcher atop accepted `2e8efb`; syntax, missing-arg paths, decoded stub payload, exact resources, 39 tests, lock/diff, and clean state passed. | Route exact immutable commit to Auditor 1; no execution or integration yet. |
| R10-V2-CENSUS-LAUNCHER-ACCEPTANCE | **Queued — Auditor 1** | Stable SHA and parent are available. | Independently accept or reject before integration/publication. |

## R9 virtualenv seed boundary

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-X2B-TOOLING-EVIDENCE-REBOOTSTRAP | **Needs correction — safe preflight stop** | Virtualenv 20.26.0 embeds pip 24.0, setuptools 68.0.0/69.5.1, and wheel 0.42.0/0.43.0. No network, fresh evidence root, repo edit, or commit occurred. | The explicit seed-setuptools 83.0.0 condition failed; do not rebootstrap or resume R9-C1. |
| R9-A2-VIRTUALENV-SEED-ISOLATION | **Accepted — no ED amendment** | Both Architects select the exact pinned creator with mandatory no-seed/no-download/no-periodic-update, empty pre-bootstrap inventory, and isolated hash-verified pip 25.0 bootstrap. | Human must narrowly ratify inert embedded seed bytes before evidence acquisition or implementation resumes. |
| R9-D2-UNSEEDED-VIRTUALENV-CREATOR-RATIFICATION | **Accepted** | Inert embedded seed bytes may remain only with mandatory no-seed isolation; ensurepip is forbidden; post-bootstrap inventory must be exactly pip 25.0; wheel is pinned at 0.45.0. | R9-X2C evidence acquisition may resume, followed by R9-V0B and only then implementation. |
| R9-X2C-TOOLING-EVIDENCE-REBOOTSTRAP | **Queued — Engineer 2** | Human authority covers one corrected networked Linux/CPython-3.11 evidence acquisition; no repository, Tamia, bundle, or production environment mutation. | Produce immutable exact closure evidence for independent acceptance. |

## R10 launcher acceptance and R9 evidence correction

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R10-V2-CENSUS-LAUNCHER-ACCEPTANCE | **Accepted** | Independent detached audit confirmed exact parent/scope/blob, launcher contract, decoded stub, 39 focused and 714 full tests, Ruff/lock/Bash/diff/clean state. | Commit `9d90ef6` is accepted locally; integration/publication/execution remain separate. |
| R9-X2C-TOOLING-EVIDENCE-REBOOTSTRAP | **Needs correction** | Exact artifacts/overlaps were retained, but resolver creation implicitly used ensurepip and `pip --upgrade`; verification removed pip/wheel script paths before empty-state proof. | Preserve the root as failed evidence; rerun through a fresh unseeded resolver and unsanitized pre-bootstrap proof. |
| R9-X2D-TOOLING-EVIDENCE-PROCEDURE-CORRECTION | **Queued — Engineer 2** | Existing authority and exact artifact set permit a clean procedural replacement without repository or cluster mutation. | R9-V0B remains blocked until a compliant immutable evidence root exists. |

## R9 corrected tooling evidence

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-X2D-TOOLING-EVIDENCE-PROCEDURE-CORRECTION | **Complete — verification pending** | D3 has 45 files, exact 15 artifacts, unseeded resolver/verifier, verified pip-only bootstrap, exact inventories, final pip check, overlap/origin/tag evidence, and preserved D1/D2 digests. | Route immutable evidence root to Auditor 2; do not implement R9-C1 yet. |
| R9-V0B-TOOLING-EVIDENCE-ACCEPTANCE | **Queued — Auditor 2** | Stable path and inventory digest `f9cc733a...55f0` supplied. | Accept or reject tooling evidence independently before tooling-lock/builder implementation. |

## R9 D3 audit correction

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-V0B-TOOLING-EVIDENCE-ACCEPTANCE | **Needs correction** | Closure, overlaps, origins, uv, nested seeds, inventories, and offline install passed; pre-import executed-byte binding and non-overwriting phase-3 failure retention did not. | D3 cannot authorize the tooling lock or R9-C1. |
| R9-X2E-IMMUTABLE-BOOTSTRAP-EVIDENCE | **Complete — verification pending** | D4 retains read-only D3 proof, verified private pip snapshots, attempts `0001`/`0002`, explicit inventory framing, exact D3 artifact equality, and successful offline inventories/checks. | Route immutable D4 to Auditor 2; no implementation authority yet. |
| R9-V0C-TOOLING-EVIDENCE-ACCEPTANCE | **Accepted** | D1-D4 identities were stable; D4 framing, read-only D3 mount, distinct attempts, verified private pip execution, exact closure/provenance, and all pip checks passed independently. | D4 may author the tooling lock; Engineer 2 may resume bounded local R9-C1. |

## R9-C1 implementation candidate and Lodestar parallel inspection

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-C1-ED36-BUNDLE-BUILDER-AND-PIP-CHECK | **Engineer complete — verification pending** | Commit `c847e075f87a2f5cb871c59d8e94d06c9ec00280`, parent `4bf0fd8`, changes exactly five authorized paths; 7 builder, 75 environment, 2 setup, and 723 full tests passed with Ruff/lock/Bash/diff clean. | Route exact commit to Auditor 2; no external construction or execution authority yet. |
| R9-V1-ED36-BUNDLE-BUILDER-AUDIT | **Active/ready — Auditor 2** | Stable candidate and prior audit matrix are available. | Acceptance gates publication and later external bundle construction authority. |
| R11-P1-LODESTAR-ED19-READONLY-MAP | **Complete — needs architecture** | Lodestar has an Anthropic judge/CLI but lacks `lodestar.interlab`, capability/perplexity computation, immutable source identity, Interlab repeat/QC and instrument provenance, and compatible packaging. | Route R11-A1; no live judging authority. |
| R11-A1-ED19-LIVE-LODESTAR-CAPABILITY-BOUNDARY | **Complete — Human ratification required** | Both Architects converge on a pinned one-shot off-cluster judge, Interlab-owned capability, content-addressed file handoff, and atomic A9′ join; ED-37 is required. | Route R11-D1 before any Lodestar or Interlab implementation. |
| R11-D1-ED37-EXTERNAL-JUDGE-BOUNDARY-RATIFICATION | **Accepted** | ED-37 and the separate off-cluster instrument, v2 schemas, immutable release preparation, QC/retention/security, and offline-only implementation boundary are ratified. | Offline preparation may proceed after the narrower temperature/instrument ruling; no credentials or paid calls. |
| R11-D1A-JUDGE-TEMPERATURE-SUFFICIENCY-PROVENANCE | **Active — Human/Researcher** | Historical judge used temperature 0, so α≥0.91 measures repeat determinism rather than independent stability; sufficiency artifacts lack prompt_version but permit a caveated reconstructed manifest. | Decide instrument continuity, urgent prose correction, retrospective manifest, and reported/non-gating coherence before R11-D2. |

## R9-V1 builder audit correction

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-V1-ED36-BUNDLE-BUILDER-AUDIT | **Needs correction** | Scope/D4 lock/broad suites passed, but target/source binding, derived-build isolation, torch authorization, staging/finalization, pip snapshot, external revision, atomic no-overwrite, TL contamination, rollback, and audit-matrix coverage failed. | Do not integrate, publish, capture a real target, or construct a bundle. |
| R9-C2-ED36-BUILDER-SEMANTIC-HARDENING | **Active/ready — Engineer 2** | Auditor supplied concrete implementation and deterministic-test gaps; correction stays within the same five paths. | Produce a new immutable successor for R9-V2. |
| R9-V2-ED36-BUILDER-REACCEPTANCE | **Blocked/ready — Auditor 2** | Await stable R9-C2 commit. | Independent acceptance still gates all external ED-36 work. |

## R9-C2 intermediate correction

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-C2-ED36-BUILDER-SEMANTIC-HARDENING | **Needs correction — clean intermediate commit** | `ea65a8711d7313e0942b75ac84636b04c901fe6e` improves binding, extraction, staging, promotion, manifests and coverage, but admits remaining network/finalization/race/TL/rollback/wrong-HEAD gaps and a stub-authority exception. | Do not audit, integrate, publish, or execute it. |
| R9-D3-SETUP-TEST-SCOPE-EXPANSION | **Accepted** | `tests/test_slurm_setup_env.py` is the sole added path; no external caller breaks. Success tests must use their real fixture HEAD, never the all-zero magic value. | Engineer 2 may resume R9-C3 locally. |
| R9-C3-ED36-FINAL-MATRIX-CLOSURE | **Complete — verification pending** | `490ae73e04cc5bfbfb814746aeab609e5e4f06fb` reports strict real-HEAD setup authority, network/finalization/no-clobber/TL/rollback hardening, 23 builder tests, 9 setup tests, and 749 full tests. | Route exact candidate to R9-V2; no integration or execution yet. |
| R9-V2-ED36-BUILDER-REACCEPTANCE | **Active/ready — Auditor 2** | Stable successor and complete commit chain are available. | Audit from a fresh exact-SHA worktree; acceptance still gates all external work. |

## R9-V2 second builder audit

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R9-V2-ED36-BUILDER-REACCEPTANCE | **Needs correction** | Exact scope, setup authority, D4 lock, Windows junction, broad suites and static gates passed; real derived-wheel trust is inconsistent, isolation fails open, cross-binding/no-clobber/cleanup/pip/TL and direct coverage remain incomplete. | No integration, publication, target capture or bundle construction. |
| R9-A3-DERIVED-BUILD-REQUIREMENT-SEMANTICS | **Active — Architect** | Auditor demands literal exact build requirements, while the prior contract permits declared ranges that map deterministically to exact approved tooling; real sdists may rely on ranges. | Issue one narrow binding rule before Engineer 2 changes build semantics. |
| R9-C4-ED36-BUILDER-FINAL-CORRECTION | **Blocked/ready — Engineer 2** | All non-ambiguous R9-V2 defects are concrete and remain in the same six paths. | Resume after R9-A3; then route a new immutable successor to R9-V3. |

## R10 publication

| Work item | Status | Evidence | Consequence |
|---|---|---|---|
| R10-X2-PUBLISH-ACCEPTED-T12-LAUNCHER | **Accepted** | Remote pre-state exact `4bf0fd8`; ancestry proven; ordinary SHA-to-main push; remote post-state exact `9d90ef6`; all forbidden mutations absent. | T1.2 statistics prose and census launcher are reproducibly published. Tamia remains at the prior revision until separately authorized. |
| R10-X3-TAMIA-FAST-FORWARD | **Accepted** | Tamia pre-state exact `4bf0fd8`; no conflicts; strict fast-forward to `9d90ef6`; dirty tracked blob and all 26 preflight untracked paths preserved; launcher blob/syntax exact; zero jobs/artifacts. | Missing census-launcher GAP-2 is closed on-cluster; execution remains blocked by ED-36 and explicit authority. |
